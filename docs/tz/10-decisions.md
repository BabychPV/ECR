# 10. Реєстр рішень і відкриті питання

> `D-01`…`D-99`. **Це єдине джерело істини щодо прийнятих рішень.** Якщо будь-який інший
> документ — цього ТЗ або довідкової бібліотеки — суперечить йому, правий цей
> реєстр. Зміна рішення вноситься **спочатку сюди**, потім у решту тексту.
>
> Стан на **2026-09-04**.

---

## 1. Вирішено

### 1.1 Платформа і стек

| # | Рішення | Джерело |
|---|---|---|
| **D-01** | **.NET 10 LTS** (.NET 8/9 — EOL 10.11.2026) | ТЗ §12 |
| **D-02** | **EF Core 10**; DataObjects.Net розглянуто і відхилено — його головна перевага (автоеволюція схеми) у нас свідомо не працює | `03` §3.6 |
| **D-03** | **React + TypeScript**, один SPA для адміністрування і роботи | ТЗ §12 |
| **D-04** | UI-кіт — **Mantine** (MIT): 12 із 15 областей адміністративні | FQ-2 |
| **D-05** | Масові операції — `SqlBulkCopy` + `ExecuteUpdate`/`ExecuteDelete`; ⛔ не EFCore.BulkExtensions | `15` |
| **D-06** | Кеш — без Redis: `IMemoryCache` + `IDistributedCache` на SQL | `15` |
| **D-07** | OpenAPI — `Microsoft.AspNetCore.OpenApi` + Scalar; ⛔ не Swashbuckle | `15` |
| **D-08** | Навантажувальні тести — **JMeter**; ⛔ не NBomber v5+, не k6 | `15` |
| **D-09** | Планувальник — Hangfire або Quartz.NET **за портом `IBackgroundJobScheduler`**; заміна коштує день | П-7 |
| **D-10** | Редактор коду — Monaco (альтернатива CodeMirror 6); компіляція скриптів — Roslyn | П-24, П-32 |
| **D-11** | Мови EN + RU + KZ; зберігання — JSON `…L10n` + реєстр `sys.Language` | ТЗ §12 |
| **D-12** | Жодної нової залежності без запису в реєстрі стека і перевірки ліцензії за першоджерелом | `15` §7 |

### 1.2 Ядро і межі

| # | Рішення |
|---|---|
| **D-13** | **Структура — це дані.** Фізична схема фіксована; жодного DDL під час роботи |
| **D-14** | Виняток із заборони DDL — рівно два артефакти розгортання: міграції EF Core і згенеровані вʼюхи `rpt.v_*`. Сервіс DDL не виконує ніколи |
| **D-15** | **БД не рахує.** Немає CLR; `rpt.*` — зріз без логіки; агрегації в сервісі |
| **D-16** | Опублікована версія структурно незмінна; презентаційний шар патчиться з `PresentationRevision`. Ключ кешу `v{id}:r{rev}` знімає інвалідацію як задачу |
| **D-17** | Ідентичність явна: `RowKey`, `ColumnDef.Code`, `RegistryEntryId`. `Ordinal` — лише відображення і матеріалізація діапазонів при `Publish` |
| **D-18** | Ядро не знає про AF, Excel і екологію: `Af*`/`Legacy*` — тільки в `ext`; перевіряється архітектурним тестом |
| **D-19** | Два діалекти виразів (шаблонів і методологій), спільний механізм. NCalc — обчислювач, **не** парсер нашої мови |
| **D-20** | Сервер авторитетний; клієнтський обчислювач — підказка, з тестом еквівалентності |

### 1.3 Зберігання

| # | Рішення |
|---|---|
| **D-21** | Модель комірок — **нормалізована базова** + гейт BR-07. Перехід на гібрид — **вибірковий по таблицях** (`StorageMode`), не глобальний. Ціна гібрида названа вголос: втрата складеного FK і затримка виявлення пошкоджень до 24 год |
| **D-22** | Партиційний ключ — **`PeriodKey` = `Year*100 + Sequence`** (для `Monthly` збігається з `YYYYMM`, для `Quarterly` — `YYYY01…YYYY04`, для `Yearly` — `YYYY01`); PK `(PeriodKey, TableRowId, ColumnDefId)` без сурогатного `Id`. Виводити місяць із `PeriodKey` арифметикою заборонено |
| **D-23** | Архівація — `INSERT…SELECT` + звірка трьох контрольних сум + `TRUNCATE … WITH (PARTITIONS)`. **`SWITCH PARTITION` не застосовний** (різні файлові групи і тип індексу) |
| **D-24** | Зупинка на архівацію **не потрібна**; потрібне вікно низької активності. Операція відновлювана; дані джерела не видаляються до збігу сум |
| **D-25** | **Retention: нічого не затирається.** Автоматичного видалення **первинних і журнальних** даних немає ніде. Обсяг керується рівнями зберігання, а не строками. Межа дії правила — D-71 |
| **D-26** | Аудит партиціонується по `ChangedAt` і архівується **щомісячно** (`aud.CellChange` ≈ 2× від `CellValue`) |
| **D-27** | Кроки розрахунку: керуємо **рівнем трейсу** (`Off`/`ErrorsOnly`/`Full`), а не строком зберігання. Відтворюваність забезпечує іммутабельний зріз при поданні |
| **D-28** | **Редакція SQL Server: Standard 2016 SP1+ — базовий режим**; Enterprise вмикається автоматично. Редакція впливає лише на операційні стратегії, ніколи — на модель і числа (АРХ-7) |
| **D-29** | `READ_COMMITTED_SNAPSHOT` обов'язковий; рівень `SNAPSHOT` не використовується; жодних довгих транзакцій |
| **D-30** | Тип зберігання емісій — `decimal(28,10)`; **`float` у результатних таблицях заборонений** |
| **D-31** | Результати розрахунку — в `calc.CalculationResult`, не в `doc.CellValue`; у документ потрапляють через `cfg.CalculationBinding` |
| **D-32** | **≥2 інстанси застосунку** незалежно від того, що виділить інфраструктура |

### 1.4 Безпека і процес

| # | Рішення |
|---|---|
| **D-33** | Два провайдери автентифікації → **одна** cookie; авторизація не знає, як користувач увійшов |
| **D-34** | Доступ до зовнішніх джерел — сервісний обліковий запис; Kerberos-делегування не використовується |
| **D-35** | Ролі — дані; функціональні права + ресурсні гранти з успадкуванням; **`IsDeny` виграє завжди** |
| **D-36** | Усі рішення про доступ — через `IAccessDecisionService`, який повертає **причину**, не `bool` |
| **D-37** | Аудит — `ChangedByUserId` (FK на `sec.User`), **не SID**: у локальних користувачів SID немає |
| **D-38** | Гранулярність затвердження — **аркуш × період** |
| **D-39** | Публікація версії методології — **workflow обов'язковий**: diff результатів → рев'ю другою особою → дата чинності. Закриті періоди автоматично не перераховуються **ніколи** |
| **D-40** | `Calculation.EditScript` і `Calculation.Publish` — різні права різних людей; публікація автором останньої правки заборонена системно |
| **D-41** | Офлайн-режим — **ні**; потребу закриває маршрут «експорт xlsx → правки → імпорт із diff» |
| **D-42** | Стан періоду рахує `PeriodStateJob`; offsets на рівнях глобально → проєкт → період → роль; пізні зміни — `IsLateEdit` |

### 1.5 Інтеграція, звітність, перехід

| # | Рішення |
|---|---|
| **D-43** | PI AF — **джерело даних**, не цільове сховище. Система обліку — SQL |
| **D-44** | **Зворотної публікації в AF немає взагалі** — ні даних, ні довідників, ні на перехідний період. `IExternalDataSink` не реалізується; `;`-серіалізація лишається утилітою валідації міграції |
| **D-45** | Наслідок D-44: споживачі AF **мають бути переведені** на SSRS/`rpt.*` або API. **Виконано порожньо (`D-107`): інших споживачів немає.** Довідники в AF для ще не мігрованих розділів лишаються під чинним процесом супроводу |
| **D-46** | PI SQL DAS (RTQP) — **у продуктиві** (63 land-процедури); read-only, ODBC, без Kerberos-делегування |
| **D-47** | Два транспорти (RTQP і Web API) — налаштування джерела, не гілка коду |
| **D-48** | Збір — **ідемпотентний + catch-up + журнал покриття**; розклад на сутність, частота природна |
| **D-49** | Довідники: master — **ECR, поетапно** через `SourceKind`; перемикання лише поза відкритим періодом, з періодом подвійної звірки |
| **D-50** | Словник потоків FLERT — **в ECR**; файл `Add Stream` read-only |
| ~~D-51~~ | ~~`AF_CurrentReportPeriod` — ECR стає його писарем~~ — **скасовано 2026-09-03**, замінено на **D-77**: поточний період є нашою конфігурацією проєкту; у AF нічого не пишеться (це суперечило б D-44) |
| **D-52** | **SSRS лишається звітною системою** на нових структурах через `rpt.*`. Веб-переглядач і конструктор звітів **виходять з обсягу** |
| **D-53** | `rpt.*` — **постійний публічний контракт**: стабільні імена і типи, версійність вʼюх. Доступ до звітів лишається в SSRS. Фільтр за статусом зрізу — D-65 |
| **D-54** | Розрахункові модулі — **частина першої черги** для розділів, що переходять, а не «наступна фаза» |
| **D-55** | Порядок формул методології не зберігається — топологічний, при `Publish` |
| **D-56** | **Water і Waste не деплояться старим способом** — вони не в проді, отже стають **першим розділом нової системи**. Вертикальний зріз = перший реліз |
| **D-57** | Те саме для трьох незадеплоєних водних держформ |
| **D-58** | Співіснування розділу — **рівно два звітні періоди** |
| **D-59** | Шаблони AF після cutover — **вимикати (disable), не видаляти**, тримати рік |
| **D-60** | Legacy-аркуші поза потоком збереження (`9c`, `HSE_373/400/401_Air`) **не переносяться** |
| **D-61** | Мобільний клієнт, SAP — не робимо |

### 1.6 Об'єм і продуктивність (факти замовника)

| # | Рішення |
|---|---|
| **D-62** | Документів на рік — **≥ 200** (мінімум, факт замовника). Цільовий сценарій гейта BR-07 — **300**, запас на зростання — **500** |
| **D-63** | **Повний річний перерахунок — бюджет ≤ 10 хвилин.** Базова лінія чинної системи — **20 хвилин**; нова система має бути **щонайменше вдвічі швидшою**, а не «не гіршою». Це вимога, а не результат заміру (`08` ПРД-13) |
| **D-64** | Бюджет відгуку (`08` §8.2) перевіряється **в режимі Standard**; Enterprise дає запас, а не умову |

### 1.7 Рішення від 2026-09-03 (друга сесія перевірки)

| # | Рішення | Знімає |
|---|---|---|
| **D-65** | **Зріз звітності має статус** `Draft` / `Approved` / `Submitted`, успадкований від даних. У `rpt.*` потрапляють **усі** зрізи — щоб числа можна було перевірити **до** затвердження. Звіти для регулятора читають лише `Approved` і `Submitted`; **фільтр — у вʼюсі `rpt.v_*`, не в RDL** | суперечність A1 |
| **D-66** | **DDL виконують SQL Agent і збережені процедури під окремим principal**: `ALTER PARTITION FUNCTION … SPLIT`, `TRUNCATE … WITH (PARTITIONS)` при архівації, генерація `rpt.v_*`. Застосунок **перевіряє стан і алертить**, ініціює архівацію викликом процедури, але DDL не виконує. Обліковий запис застосунку лишається без DDL-прав у PROD. Регламент SQL Agent і його моніторинг — **частина постачання** | суперечність A4 |
| **D-67** | **Поданий документ не редагується.** Правка вимагає явного `Reopen` документа з причиною → `Draft` → повторне подання → **новий** зріз. Старий зріз лишається `Submitted` назавжди. `Grace` дає час на правки **неподаних** документів, а не право змінити подану форму. `Reopen` документа неможливий, поки період у `Closed` | неоднозначність B1 |
| **D-68** | **Час:** зберігання — UTC у **`datetime2(3)`** (уточнено D-85); межі періодів, offsets у днях і `IsLateEdit` рахуються в **поясі майданчика** (`Project.TimeZoneId`); відображення — теж у поясі майданчика, однаково для всіх. `PeriodStateJob` працює за цим поясом, не за UTC-опівніччю | неоднозначність B5 |
| **D-69** | **Розділення обчислених значень:** формули шаблону → `doc.CellValue` з `IsCalculated = 1`; результати методологій → `calc.CalculationResult`, у документ потрапляють посиланням через `cfg.CalculationBinding` | суперечність A6 |
| **D-70** | `IsLateEdit` позначає: (а) зміни неподаних документів у `Grace`; (б) будь-яку зміну після `Reopen` | — |
| **D-71** | **Межа правила «нічого не затирається»:** воно стосується даних, які неможливо відтворити (`doc.*`, `aud.*`, `calc.CalculationResult` і записаний трейс, `ext.*`, `itg.*`, `arc.*`). **Похідні артефакти прибирати можна**: зрізи `rpt.ReportSnapshot` (крім `IsSubmitted` і `IsCurrent`), кеші, тимчасові файли експорту | суперечність A2 |
| **D-72** | Ідентифікатори рішень — **`D-NN` (латиниця)**; `B-`, `I-`, `S-`, `T-`, `K-`, `N-`, `R-`, `FQ-` лишаються за відкритими питаннями. Кирилична `Р-` більше не використовується | суперечність A7 |

### 1.8 Одиниці вимірювання і поточний період (2026-09-03)

| # | Рішення |
|---|---|
| **D-73** | **Реєстр одиниць вимірювання — окремий механізм `uom`** (`Dimension`, `Unit`, `Conversion`), а не звичайний реєстр. Причина: одиниця — це **метадані структури** (колонка, константа, результат), на неї посилається структура, а не комірка; плюс конверсії мають арифметичну семантику, яку рушій перевіряє при публікації |
| **D-74** | **Неявних конверсій не буває.** Рушій ніколи не конвертує сам — тільки за явним `CONVERT(...)` у виразі або правилом мапінгу `SourceUnit → TargetUnit`. Конверсія між різними розмірностями — **помилка**, не спроба «через базу» |
| **D-75** | **Контекстні коефіцієнти не є конверсіями одиниць.** Щільність, теплотворність, молярна маса залежать від речовини й умов і змінюються з часом → `calc.MethodologyConstant` з власною одиницею, темпоральністю і версійністю. У `uom.Conversion` вони заборонені (і неможливі за побудовою) |
| **D-76** | **Перевірка одиниць при `Publish`** — відхилення публікації, якщо величини в різних одиницях поєднані без явної конверсії, агрегуються рядки з одиницею на рядок без приведення, або результат оголошений у несумісній розмірності. Це механізм, що ловить «тихо неправильні числа» до продуктиву |
| **D-77** | **`Project.CurrentPeriod` — наша конфігурація**, режими `Auto` (веде `PeriodStateJob`) і `Pinned` (адміністратор, з причиною і аудитом). Повністю замінює `AF_CurrentReportPeriod`: з AF не читається, у AF не пишеться. Використовується як **значення за замовчуванням** (документи, розклади, параметри звітів) і **ніколи** — для рішень про доступ, які спираються на стан періоду |
| **D-78** | **`MethodologyVersion.CalendarMode`** визначає джерело `Period.Days`/`Hours`/`Seconds`: `Actual` (типовий) або зафіксована legacy-конвенція. Різниця конвенцій змінює **всі** числа при перерахунку в `г/с` і `т/рік`, тому режим видимий у конфігураторі й обов'язковий у diff при публікації |
| **D-79** | Одиниці на межі інтеграції: `ext.EntityFieldMap` зберігає `SourceUnitId`/`TargetUnitId`; конверсія — **при завантаженні**, з журналом; **сирі дані в `ext.*` лежать в одиниці джерела**, інакше повторний перерахунок з архіву дасть інший результат. Зміна UOM атрибута в джерелі **зупиняє збір**, а не конвертує «як здається» |

### 1.9 Модель реалізації (2026-09-03)

| # | Рішення |
|---|---|
| **D-80** | **Весь обсяг реалізує AI — Claude.** Питання складу команди розробки (N-3) знято; питання обсягу (N-2) переформульовано: послідовність «зріз → Water/Waste → нарощування партіями» зберігається, але керує **ризиком і черговістю верифікації**, а не економією розробницьких тижнів. Деталі — [`11-ai-delivery.md`](11-ai-delivery.md) |
| **D-81** | **Обов'язкові людські ролі приймання**: технічний власник (рев'ю, злиття, арбітр змін до цього реєстру), методолог / власник золотого набору, DBA, PI-адмін, ІБ, бізнес-приймальник. Перші дві — **з прізвищами до старту**. Це не команда розробки, а ті, без кого AI не має на що спертися |
| **D-82** | **Протокол роботи AI — блокуючий** (`11` §11.4): джерело істини — цей реєстр; відкриті питання AI не вирішує; жодних вигаданих фактів про середовище; кожна зміна посилається на ФВ-/D-/АРХ-номер; числа перевіряються даними, не рев'ю; нова залежність — лише через реєстр стека; архітектурні тести, перевірка ліцензій і бюджет продуктивності — блокуючі в CI |
| **D-83** | **Оцінка строку формується як сума календарних обмежень**, а не «команда × місяці»: факти від організації, провізіонування середовищ, ручна звірка bootstrap, золотий набір, цикл числової звірки, UAT, два періоди співіснування на партію розділів. Орієнтир: перший продуктивний реліз — 3–4 місяці, повний перехід — 10–14; майже весь строк — верифікація і перехід, не код |

### 1.10 Рішення реалізації (2026-09-04, пакет `build/`)

> Прийняті під час підготовки пакета для розробки. Кожне усуває конкретну
> суперечність або неоднозначність, знайдену при спробі написати з ТЗ код.
> Детальне обґрунтування — [`../build/01-TOR.md`](../build/01-TOR.md) §4–6.

| # | Рішення | Усуває |
|---|---|---|
| **D-84** | **Фізичний ключ даних документа:** `doc.TableRow` — `PK (PeriodKey, Id)`, `Id` із `SEQUENCE` (не `IDENTITY`), `UQ (PeriodKey, TableInstanceId, RowKey)`; `doc.CellValue` — `PK (PeriodKey, TableRowId, ColumnDefId)` без сурогатного `Id`, складені FK на рядок і на колонку. Редакція в `reference/design/07-data-model.md` була застарілою | суперечність з `B02` §2 |
| **D-85** | **Тип часу — `datetime2(3)` в UTC**, не `datetimeoffset`. Зсув не несе інформації (один пояс майданчика), тип вужчий і краще індексується на 108 млн рядків. Обчислення меж — через `TimeZoneInfo` за `Project.TimeZoneId`; `DateTime.Now` заборонений, час лише з `IClock` | `D-68` проти DDL у `B02`/`B04` |
| **D-86** | **Автор дії — `…ByUserId int` → `sec.User(Id)` в усіх без винятку таблицях** аудиту і soft delete. `…BySid` не використовується ніде. Окремо: `sec.User.Sid` (ідентифікатор доменного користувача) і `PrincipalSid` (AD-група) — це **не** авторство і лишаються | залишки SID у трьох документах |
| **D-87** | **`doc.CellValue.ValueUnitId`** — окрема колонка під одиницю на рядок (`DataType = Unit`), з `CHECK` на решту типів. Раніше для неї не було місця | прогалина ФВ-16.8 |
| **D-88** | **`IExternalDataSink` не створюється навіть як порожній інтерфейс.** Порожній контракт «на майбутнє» — запрошення його колись реалізувати | уточнює `D-44` |
| **D-89** | **Формат коду сутності:** `^[A-Za-z][A-Za-z0-9_]{0,63}$` для `Template/Sheet/Table/Column/Registry/Unit/Methodology/Permission`. Обмеження продиктоване лексером: код вживається всередині `[...]` без екранування. `RowKey` — окреме правило `^[A-Za-z0-9_.\-]{1,100}$`, бо допускає цифрові ключі і GUID | прогалина: формат не був заданий |
| **D-90** | **Валідація блокує запис лише на рівні комірки.** Рівні рядка, таблиці й документа не блокують збереження — вони блокують `Submit`. Заборона зберегти проміжний стан зробила б роботу з великою таблицею неможливою | неоднозначність: коли валідація блокує |
| **D-91** | **Реалізація за пакетом `build/`.** Точка входу — [`../build/00-START-HERE.md`](../build/00-START-HERE.md); контракти — `02-contracts.md` (+`02a`, `02b`, `02c`); скелет — `05*`; тести — `06*`; етапи і промпти рев'ю — `07-checkpoints.md`. При розбіжності в **деталі реалізації** правий контракт; при розбіжності в **рішенні** правий цей реєстр | — |

### 1.11 Рішення від 2026-09-04 (наскрізна перевірка узгодженості)

> Прийняті після зіставлення `tz/` з `build/`, яке виявило чотири вимоги без
> механізму і кілька розбіжностей у формулюваннях.

| # | Рішення | Усуває |
|---|---|---|
| **D-92** | **Рядкового фільтра в гранті немає.** `RowFilterExpression` виходить з обсягу: він вимагав би **третього** діалекту виразів і компіляції в SQL-предикат (`B05` §187 — фільтрація в пам'яті зруйнувала б бюджет читання зрізу), тоді як система будується на двох діалектах і одному парсері (`ФВ-9.5`). Обмеження видимості лишається на рівнях проєкт → аркуш → таблиця → колонка. `EditDenyReason.RowFiltered` не додається | вимога без механізму; суперечність із `ФВ-9.5` |
| **D-93** | **`doc.Document.Status` не існує.** Єдине джерело істини про робочий стан — `wf.ApprovalState` за `(DocumentId, SheetDefId, PeriodKey)` (`D-38`). Скалярний статус на документ був би другим джерелом, яке рано чи пізно покаже `Approved` на документі з половиною аркушів у `Draft`. Зведений стан для списку документів **рахується запитом**, а не зберігається | два джерела істини |
| **D-94** | **Історичні дані мігруються виключно з дзеркал `dbo.AF_*`** (`ФВ-15.4`). З `.xlsm` вичитується лише **структура** (`ФВ-2.10`). Імпорту даних із legacy-файлів немає і інструмента для нього не створюється: два джерела тих самих чисел дали б два різні результати міграції і жодного способу сказати, який правильний | суперечність із `ФВ-15.4` |
| **D-95** | **Рядки інтерфейсу зберігаються на сервері** (`sys.UiString` + версія каталогу як ETag). Без цього обіцянка `ФВ-14.9` («додати мову = запис у реєстр, не збірка клієнта») невиконувана: назви аркушів прийшли б із БД, а меню й кнопки лишилися б у збірці. Відсутній переклад підмінюється мовою за замовчуванням, відсутній ключ показується як ключ — одна забута локалізація не ламає екран | невиконувана вимога |

| **D-96** | **Симуляція «очима користувача» — лише читання.** Будь-який запис під нею відхиляється (`SimulationReadOnly`) незалежно від прав суб'єкта; сеанс із причиною пишеться в `aud.SimulationSession`, автором дій лишається той, хто симулює; UI показує банер увесь сеанс. Без аудиту симуляція стала б способом безслідно переглянути чужі дані | `ФВ-6.16a` не мала механізму |
| **D-97** | **Bootstrap-адміністратор із одноразовим паролем.** Один, лише локальний (унікальний індекс), `MustChangePassword` до першої зміни — доти дозволені лише зміна пароля і вихід; **автоматично вимикається**, щойно з'явився активний доменний адміністратор. Не видаляється — потрібен в аудиті | `ФВ-6.18` не мала механізму |
| **D-98** | **`Orphaned` обчислюється нічною перевіркою інваріантів (`ФВ-7.7`) і при зміні вікна дії запису реєстру** — і зберігається як `doc.TableRow.IsOrphaned`. **Не при читанні зрізу**: перевірка чинності на кожен рядок при кожному відкритті таблиці зруйнувала б бюджет 400 мс | `ФВ-8.13` не мала ні носія, ні моменту |
| **D-99** | **Проєкти і періоди не видаляються** ніколи — життєвий цикл закінчується `Archived`. Наслідок `D-25`: на них посилаються дані, аудит, зрізи і результати розрахунку | прогалина: видалення не було описане |

Типові рішення, застосовані без окремих номерів: одночасна публікація версії
розв'язується оптимістичним блокуванням по `RowVersion` (`ФВ-2.9`); членство в
AD-групах береться з токена входу, недоступність каталогу блокує лише нові
доменні входи (`ФВ-6.15a`); фонові задачі — три спроби з експоненційною
паузою, далі остаточний `Failed` і алерт (`ФВ-12.4a`).

Супутні уточнення тієї самої перевірки, без окремих номерів: рівні гранта —
**шість** (`None → Read → Write → Submit → Approve → Manage`), як у контракті;
`Sequence` періоду обмежений `1…12` через межі партиційної функції
(`ФВ-1.5a`); округлення `AwayFromZero` і відхилення значень понад `Scale`
піднято з `build/` у ТЗ (`ФВ-9.16a`, `ФВ-9.16b`); `Reopen` виконується в
гранулярності аркуш × період; `Archived` — кінцевий стан і не збігається з
рівнем зберігання `arc.*`; документ у глосарії — наскрізний по періодах.

### 1.12 Рішення і факти від 2026-09-04 (відповіді замовника)

| # | Рішення | Закриває |
|---|---|---|
| **D-100** | **Технічний власник і власник золотого набору — оператор ПК-2**, тобто та сама людина, що запускає реалізацію. Формально ролі закриті; залишковий ризик названо нижче | `H-1`, `R-4` |
| **D-101** | **Середовище: SQL Server 2019 RTM CU32 (15.0.4480.2), Enterprise Core-based, `EngineEdition = 3`** на всіх трьох серверах (`NCATUATV12`, `NCATDEVV08`, `NCATSQLV92\PI_ECR`). Підлога `Standard 2016 SP1` виконана із запасом; блокера немає. **АРХ-7 лишається без змін**: покладатися на Enterprise не можна, але якщо він є — використовується автоматично | `B-1` |
| **D-102** | **RCSI на чинній базі `ECR` вимкнено** (`AllowSnapshotIsolation` теж). Нас це не зачіпає: ми створюємо **власну** базу `Ecr`, і скрипт розгортання `06-rcsi.sql` вмикає `READ_COMMITTED_SNAPSHOT` при створенні. Перевірка при старті лишається `Critical`. Чинну базу `ECR` не чіпаємо | `B-2` (основна частина) |
| **D-103** | **Навантажувальні заміри виконуються з примусовим `Database:EditionMode = "Standard"`**, бо фізичного Standard-інстансу в контурі немає — усі три Enterprise. Це не обхід `D-64`, а єдиний спосіб його виконати: бюджет має витримуватися на базовій редакції | суперечність: `D-64` вимагав Standard, якого немає |
| **D-104** | **Аутентифікація першого релізу — Windows (Negotiate) + базова локальна.** `P-1` (політика паролів, 2FA) і `P-2` (SMTP) **відкладені**: TOTP не робимо, самостійного скидання пароля немає, пароль скидає адміністратор. Базова політика (мінімальна довжина, блокування після N невдач) лишається, бо вона не потребує рішень ІБ | `P-1`, `P-2` — відкладено, не закрито |
| **D-105** | **Методологія змінюється раз на рік, формули — частіше.** Отже **рівень 2 (C#-скрипти) не входить у перший реліз**: він лишається описаною точкою розширення, але не будується. `K-1` (дозвіл ІБ) з критичного шляху знято. Остаточно підтвердить класифікація `C-1`/`K-4` на Етапі 0 — якщо вона знайде методології, які декларативно не виражаються, питання повертається | `K-1a`; `K-1` знято з критичного шляху |
| **D-106** | **Два режими надходження даних, а не один.** Дані з **наших веб-форм** не опитуються взагалі: вони приходять записом і одразу запускають інкрементний перерахунок (`ФВ-12.1`, латентність `< 10 с`). Опитування за розкладом стосується **лише адаптера до PI AF**. Частоти по сутностях — конфігурація Етапу 5 (`ФВ-13.15`), а не код | `S-2` в принциповій частині |

| **D-107** | **Інших споживачів PI AF немає** — крім чинної системи, у AF ніхто не пише і нічого не читає (факт замовника). Отже `D-45` виконано **порожньо**: переводити нема кого, і пункт runbook «перевести споживачів AF» зводиться до запису «споживачів немає». Ризик мовчазного застигання даних у чужих дашбордах знято | `I-1` |

### 1.13 Рішення від 2026-09-04 (третя перевірка узгодженості)

| # | Рішення | Усуває |
|---|---|---|
| **D-108** | **`Period.Sequence` обмежений 1…12**, `CK_Period_Seq` виправлено з `1..99`. Верхня межа не довільна: партиційна функція перелічує межі як `YYYY01…YYYY12`, і `Sequence = 13` мовчки ліг би в грудневу партицію. Покриває `Monthly` (1–12), `Quarterly` (1–4), `Yearly` (1) і `Custom` до 12 періодів на рік | суперечність: ТЗ казало 1–12, DDL дозволяв 1–99 |
| **D-109** | **Значення понад `Scale`: округлення при вставці й імпорті, відмова при ручному введенні та API.** При вставці округлення `AwayFromZero` **із показом переліку змінених комірок** до підтвердження. Мовчки не округлюється ніде. Різниця свідома: вставка 500×60 з Excel — головний шлях введення, і відхилення сотень комірок зробило б його непридатним | неоднозначність: `ФВ-9.16b` проти `ФВ-14.4` |
| **D-110** | **`Project.TimeZoneId` незмінний після відкриття першого періоду.** Зміна дозволена лише доки проєкт у `Draft`. Ретроактивна зміна зсунула б межі вже **закритих** періодів і переписала б `IsLateEdit` на вже **поданих** формах | прогалина: змінність не була описана |
| **D-111** | **Механізм локалізації один.** Повідомлення каталогу помилок — записи того самого `sys.UiString` із префіксом `err.<код>`. Окремого сховища текстів помилок немає | прогалина: два сховища текстів однієї природи |
| **D-112** | **Тривалість періоду обчислюється, не зберігається.** `Days`/`Hours`/`Seconds` виводяться з меж періоду і `CalendarMode` **тієї методології, що рахує**; колонки на `doc.Period` немає. Інакше дві методології з різними режимами конфліктували б за одне поле | неоднозначність `ФВ-16.11` |

| **D-113** | **Редактор виразів будується зараз, режим C# — потім.** Область 10 UI перенацілена зі «скриптів рівня 2» на **редактор виразів нашої мови**: Monaco, автодоповнення, перевірка при введенні, прогін `calc.TestCase` із редактора. Діалект — параметр компонента, тому додавання режиму C# пізніше не потребує переробки UI. `D-105` лишається чинним, `K-1` лишається поза критичним шляхом. Залежність `Microsoft.CodeAnalysis.CSharp.Scripting` **прибрано** з `Directory.Packages.props`: тримати рушій виконання чужого коду під нереалізовану функцію суперечить `D-12` | суперечність: `D-105` проти області 10 і 4–5 тижнів FE |

| **D-114** | **Каталог UI-рядків поділено на `Public` і `Private`.** Анонімно віддається лише публічна область (вхід, chrome, помилки автентифікації); підписи адміністративних областей і назви прав — після входу. Інакше анонімний ендпоінт розкривав би поверхню функціоналу, що суперечить `ФВ-14.2`. Кожна область має власний `ETag` | суперечність `ФВ-14.9` проти `ФВ-14.2` |
| **D-115** | **Bootstrap-адміністратор створюється з `ECR_Bootstrap__Password`** при першому старті. Пароль не має значення за замовчуванням і не потрапляє ні в seed, ні в `appsettings`, ні в лог (`ФВ-6.11`). Запис уже існує → змінна ігнорується; змінної немає → **попередження, не помилка**: це нормальний стан контуру, де вхід переведено на домен | прогалина: `D-97` описував життєвий цикл, але не створення |
| **D-116** | **Округлення при вставці показується позначкою і лічильником, без діалогу.** Вставка застосовується одразу; змінені комірки підсвічуються, над сіткою — «округлено N значень» із переходом до переліку. Діалог на кожну вставку з Excel закривали б не читаючи, і правило перестало б працювати. Позначка — **лише на змінені** комірки, інакше лічильник втрачає сенс | прогалина: `D-109` не мав механізму показу |

Супутні уточнення без окремих номерів: клієнтські JSON-словники прибрано, тип
`Language` більше не union (інакше четверта мова не скомпілювалася б); seed
додає обов'язковий рядок `UiStringRevision` і мінімальний публічний набір
англійською — `RU`/`KZ` навмисно відсутні, щоб механізм підміни мовою за
замовчуванням перевірявся з першого дня; кеш каталогу — за ключем
`{lang}:{scope}:{revision}`; тексти помилок резолвить **сервер**; профіль
симуляції **не кешується
взагалі** (інакше справжній користувач отримав би чужий профіль із прапорцем
`IsSimulation`); `IsOrphaned` **знімається** тим самим механізмом, що й
ставиться; гонка `Reopen` і `PeriodStateJob` розв'язується `UPDLOCK` на рядку
періоду.

> **Що з `D-107` випливає для міграції.** Після cutover розділу дзеркала
> `dbo.AF_*` для нього перестануть оновлюватися — але **історію вони
> збережуть**, а саме вона нам і потрібна (`ФВ-15.4`). Тому вимога проста і
> одна: **не виводити дзеркала з експлуатації, доки міграція розділу не
> звірена**. Це той самий строк, що й для шаблонів AF (`D-59`, рік), тож
> окремого регламенту не потребує.

> **Залишковий ризик `D-100`.** Технічний власник і власник золотого набору —
> це дві **різні за природою** ролі: перша приймає код, друга ручається за
> числа. Коли їх суміщає одна людина, незалежної перевірки чисел не лишається.
> Пом'якшення, яке цей ризик знімає майже повністю: **золотий набір береться з
> реально поданих форм**, а не складається вручну. Тоді джерелом істини є
> документ, який уже пішов регулятору, і роль власника зводиться до
> супроводу набору, а не до авторства чисел. Приймання розбіжностей
> (`§8.9` п. 4) усе одно потребує **методолога** — це третя людина, і вона
> потрібна лише в момент, коли розбіжність знайдено.

---

## 2. Відкрито

### 2.1 Факт, який треба з'ясувати (не рішення — запит на 15–60 хвилин)

| # | Що | Хто | Наслідок |
|---|---|-----|----------|
| **S-2a** | Частота опитування **по сутностях PI AF** | PI-адмін | принцип закрито (`D-106`); лишається таблиця частот — конфігурація Етапу 5, не блокер |
| **T-4** | Чи відкривав хтось `9c`, `HSE_373/400/401` за 12 місяців | перевірка по файловій системі | підтвердження D-60 |
| **B-2a** | Конфігурація `tempdb`: кількість файлів і розміри | DBA | не блокер; впливає на пік «останнього дня». Решту `B-2` закрито `D-102` |

**Закрито 2026-09-04:** `B-1` (D-101), `B-2` в основній частині (D-102),
`K-1a` (D-105), `S-2` у принциповій частині (D-106), `I-1` (D-107).

### 2.2 Рішення бізнесу, ІБ, юристів

| # | Питання | Хто | Рекомендація |
|---|---|---|---|
| **K-2** | Хто має `Calculation.EditScript` / `Publish` | Бізнес + ІБ | 2–3 іменовані особи; чотири очі |
| **S-4** | Хто змінює мапінг і розклад у проді | ІБ + бізнес | `Integration.Manage`, 1–2 особи, зміни версіонуються |
| **J-4** | Хто отримує алерти | Бізнес + ІБ | спершу спитати, хто дізнається про збій сьогодні |
| **R-2** | Sour Analyzers: множник до маси чи до об'єму | Методолог | не сперечатися — звірити обидва варіанти з поданими формами |
| **M-1** | **Методолог, який приймає числові розбіжності** | Бізнес | випливає з `D-100`: технічний власник і власник золотого набору суміщені, тому приймання розбіжностей потребує третьої людини. Потрібна не до старту, а до першої звірки (Етап 4) |

**Закрито або відкладено 2026-09-04:** `H-1` і `R-4` — закриті (`D-100`);
`K-1a` — закрито (`D-105`), `K-1` знято з критичного шляху; `P-1` і `P-2` —
**відкладені** (`D-104`), повертаються перед розширенням аутентифікації;
`L-1` — не блокує (дефолт Quartz під Apache-2.0), повертається лише якщо
захочемо Hangfire.

### 2.3 Роботи Етапу 0, а не питання

| # | Робота | Закриває |
|---|---|---|
| **BR-07** | Замір фізичної моделі: 300 документів, заповненість 90%, 125 RPS в одну партицію | D-21 |
| **C-1 + K-4** | Класифікація 44 класів `ECW_C**` за формою | обсяг коду; рівень 2 ≤ 5 |
| **N-1** | Конвеєр на папері для 3 найскладніших методологій | чи будуємо конвеєр |
| **J-1** | Профіль річного перерахунку по модулях | як з 20 хв зробити 10; J-2 |
| **FQ-1** | Прототип grid: paste 500×60, fill, undo ≥50, права по комірках | RevoGrid чи запасний варіант |
| **RDL-1** | Пілот на 10 звітах через `rpt.v_*` | складність перенацілювання 166 RDL |

---

## 3. Журнал змін

| Дата | Зміна | Причина |
|---|---|---|
| 2026-09-03 | D-23: архівація без `SWITCH PARTITION` | `SWITCH` неможливий між rowstore і columnstore на різних файлових групах — помилка вихідного ТЗ |
| 2026-09-03 | D-22: партиційний ключ `PeriodKey`, PK без `Id` | вирівнювання індексів, читабельні межі партицій, −0.9 ГБ/рік |
| 2026-09-03 | D-28 (АРХ-7): робота на Standard, автоввімкнення Enterprise | вимога замовника |
| 2026-09-03 | D-25…D-27 (ЗБР): нічого не затирається | рішення замовника |
| 2026-09-03 | D-44, D-45: запису в AF немає взагалі | рішення замовника |
| 2026-09-03 | D-52, D-53: SSRS лишається; веб-переглядач і конструктор звітів виходять з обсягу | рішення замовника |
| 2026-09-03 | D-56, D-57: Water/Waste — перший розділ нової системи | рішення замовника |
| 2026-09-03 | D-62, D-63: ≥200 документів; річний перерахунок ≤10 хв | факти замовника |
| 2026-09-03 | Оцінка строку переглянута на повний обсяг системи | попередня охоплювала половину |
| 2026-09-03 | D-65…D-72 за результатами другої перевірки | знято 4 суперечності і 2 неоднозначності |
| 2026-09-03 | D-73…D-79: реєстр одиниць `uom` і конверсії | прогалина C1 — без нього конверсії розповзлися б по формулах методологій |
| 2026-09-03 | D-77 замінює D-51: поточний період — конфігурація проєкту | знято суперечність A3 (запис у AF vs D-44) |
| 2026-09-03 | D-80…D-83: реалізація силами AI (Claude); ролі приймання; протокол; модель строку | рішення замовника; N-2/N-3 знято |
| 2026-09-04 | D-84…D-91 — рішення реалізації з пакета `build/` | спроба написати код із ТЗ виявила 6 суперечностей і 7 неоднозначностей |
| 2026-09-04 | D-92…D-95 за результатами наскрізної перевірки `tz/` проти `build/` | чотири вимоги не мали механізму в контрактах |
| 2026-09-04 | D-96…D-99 + чотири типові рішення | закрито останні прогалини перевірки: симуляція, bootstrap-адміністратор, `Orphaned`, видалення |
| 2026-09-04 | D-100…D-106 за відповідями замовника | закрито `H-1`, `R-4`, `B-1`, `B-2`, `K-1a`, `S-2`; відкладено `P-1`, `P-2`, `K-1`; відкрито `M-1` |
| 2026-09-04 | D-107: інших споживачів PI AF немає | закрито `I-1`; `D-45` виконано порожньо; з runbook прибрано найдовший за строком пункт |
| 2026-09-04 | D-108…D-112 за третьою перевіркою | `Sequence` 1–12, округлення при вставці, незмінний пояс, один механізм локалізації, обчислювана тривалість періоду |
| 2026-09-04 | D-113…D-116 за четвертою перевіркою | редактор виразів, поділ каталогу на public/private, створення bootstrap-адміністратора, показ округлення |
