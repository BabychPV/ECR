# progress.md — чекліст виконання

> Заповнюється **на ПК-2** після кожного модуля (не в кінці етапу).
> Це єдине місце, де видно, де ви зупинилися. Рев'ювер **не вірить** цьому
> файлу на слово — він перезапускає build і тести сам; розбіжність між записами
> тут і фактом є критичним зауваженням.

**Поточний етап:** Етап 0 (bootstrap) — **ЗАВЕРШЕНО І ВІДРЕВ'ЮОВАНО**
**Останній тег:** `stage-0-reviewed`
**Оновлено:** 2026-09-04

---

## Формат запису

```markdown
- [x] <модуль>/<файл або група> — done — <що зроблено, одним рядком>
      build: OK | tests: N passed, 0 failed
```

Незавершене — `- [ ]`. Заблоковане — `- [!]` із посиланням на `Q-NNN`.

---

## Етап 0 — BOOTSTRAP

- [x] Структура папок створена за деревом з `05-skeleton.md` — пакет документації перенесено в `docs/` (`Q-001`)
- [x] Усі файли з `05-skeleton.md` створені без змін — 302 із секцій `05*`/`06*`, 32 дослівно з `02-contracts.md`,
      8 із секцій «два файли під одним заголовком», 2 за `COPY FROM`.
      41 файл дерева не мав вмісту в `05*` — **усі створені 2026-09-04** із джерел ТЗ (`Q-015`)
- [x] Усі файли з `06-tests.md` створені без змін — разом із `06a`…`06e` і фікстурою `water-demo.json`
- [x] `git init` + commit + тег `stage-0`
- [x] `dotnet restore` — OK (після `Q-003`, `Q-004`)
- [x] `npm install` — OK, версії з `04-environment.md` §4 **без правок**
- [x] `dotnet build Ecr.sln` — **OK, 0 errors** (Debug і Release)
- [x] `npm run build` — **OK**; `npm run typecheck` — 0 помилок
- [x] Тести запускаються і падають (`Assert.Fail`), раннер працює
- [x] `09-commands.md` оновлено реальними командами, журнал §8 заповнено
- [x] commit + тег `stage-0-verified`
- [x] Звіт показано, робота зупинена

**Записів у журналі: 29.** Фактичний розподіл за типами (перевірено скриптом
проти заголовків `questions.md`, а не переписано з пам'яті):

| Тип | Кількість | ID |
|---|---:|---|
| `BOOTSTRAP-FIX` | 9 | `Q-002`, `Q-003`, `Q-004`, `Q-006`, `Q-007`, `Q-008`, `Q-011`, `Q-019`, `Q-022` |
| `DECIDED` | 8 | `Q-001`, `Q-009`, `Q-010`, `Q-012`, `Q-020`, `Q-023`, `Q-024`, `Q-029` |
| `CONTRACT` | 3 | `Q-005`, `Q-014`, `Q-026` |
| `CONFLICT` | 3 | `Q-013`, `Q-018`, `Q-027` |
| `SCOPE` | 5 | `Q-015`, `Q-017`, `Q-021`, `Q-025`, `Q-028` |
| `ENV` | 1 | `Q-016` |

**Рішення замовника:** 4 — `Q-013` (варіант A), `Q-018` (варіант B),
`Q-004` (мажор `NCalcSync`), `Q-029` (база з рядка підключення).

**Закрито за вказівкою «вирішуй відповідно ТЗ» (2026-09-04):**
`Q-014` (10 типів перенесені в `02-contracts.md`), `Q-015` (усі 41 файл створені),
`Q-018` (4 порти в контракті), `Q-026` (`MatchJson` — схема права).

**Лишається відкритим:** `Q-006` (політика аналізаторів), `Q-012` (семантика `RowDto`),
`Q-016` (немає Docker), `Q-017` (frontend поза Етапом 0),
**`Q-027` — 22 сутності розходяться зі схемою БД**; правило визначене
(права схема), виконання рознесене за етапами.

### Знайдено тестів

| Проєкт | Знайдено | Результат |
|---|---:|---|
| `Ecr.Expressions.Tests` | 142 | 142 failed |
| `Ecr.Application.Tests` | 125 | 125 failed |
| `Ecr.Domain.Tests` | 113 | 113 failed |
| `Ecr.Infrastructure.Tests` | 41 | 41 failed |
| `Ecr.Api.Tests` | 27 | 27 failed |
| `Ecr.Architecture.Tests` | 23 | 23 failed |
| `Ecr.Calculations.Tests` | 15 | 15 failed |
| **backend разом** | **486** | **486 failed, 0 passed** — очікувано |
| **`Category=Integration`** (окремо) | **119** | **119 failed** — `Ecr.Infrastructure.Tests` 116, `Ecr.Application.Tests` 3 |
| `Ecr.Web` (vitest) | 23 | 23 failed — очікувано |

**Разом 628 тестів знайдено, усі падають із `not implemented`, 0 passed, 0 skipped.**
Нуль знайдених тестів був би провалом етапу; цього не сталося.

> ⚠ **Виправлено за рев'ю (`Q-025`, К-1).** Тут раніше стояло «61 тест, не
> запускалися: немає Docker» — і те, і те неправда. Інтеграційні тести
> **запускаються і падають** на `Assert.Fail`, до `SqlServerFixture` виконання
> просто не доходить; Docker знадобиться з Етапу 1, коли з'являться тіла.
> Помилка виникла тому, що я порахував `--list-tests | grep -c` замість того,
> щоб запустити прогін. Рев'ювер запустив — і числа розійшлися.

## Етап 1 — Ядро метаданих і сховище

*(модулі — у `07-checkpoints.md`)*

**Розпочато:** 2026-09-04

- [x] 1.1 `Ecr.Domain` — значеннєві типи (`PeriodKey`, `EcrCode`, `RowKey`, `CellValueData`)
      — done — самі типи вже були реалізовані (контрактні, `05-skeleton.md` §3),
      тож модуль полягав у написанні тіл тестів замість `Assert.Fail`
      build: OK | tests: 27 passed, 0 failed (`Ecr.Domain.Tests/ValueObjects`)

Що саме перевіряють ці 27 і чому вони не формальні:

* `PeriodKey` — що `YYYYMM` збігається з ключем **лише** для місячних періодів,
  а `YearRange` для квартального дає чотири ключі, а не дванадцять. Друга
  перевірка ловить помилку, через яку архівація зачепила б вісім неіснуючих
  партицій.
* `EcrCode` — що межа рівно 64 символи (перевірено обидва боки: 64 приймається,
  65 ні) і що `TryCreate` не створює «майже код».
* `RowKey` — що цифровий ключ `"7001001"` приймається тут і **не** приймається
  як `EcrCode`; що `NewDynamic` дає 32 шістнадцяткові символи без дефісів.
* `CellValueData` — що `ValueBool = false` і `ValueNumeric = 0` є **значеннями**,
  а не порожнечею, і що `IsEmpty` разом зі значенням некоректне. Саме тут
  народжується помилка «нуль перетворився на порожньо».

**Стан прогону після модулів 1.1–1.2, 1.7–1.9:** див. таблицю нижче.

- [x] 1.7–1.9 `Ecr.Application` — use-cases метаданих, документів і аудит
      — done — `PublishTemplateVersionHandler`, `PatchPresentationHandler`,
      `PatchCellsHandler`, `CreateRowHandler`, `GetTableSliceHandler`
      build: OK | tests: 24 passed, 0 failed (`Ecr.Application.Tests` `Stage1`)

Ключові рішення модуля:

* **`PatchCellsHandler`** — порядок кроків продиктований бюджетом 300 мс:
  знімок метаданих без БД → версії рядків одним запитом → конфлікти
  збираються **всі** → права одним викликом на зріз → розкладка на три
  операції (`R-B4`) → одна транзакція → черга перерахунку **після** commit.
* **Часткове застосування неможливе ніде**: конфлікт в одному рядку відхиляє
  весь батч, структурна зміна в патчі відхиляє весь патч.
* **`GetTableSliceHandler`** — явна порожнеча передається як присутній ключ зі
  значенням `null`, незаповнена комірка не передається взагалі. Спершу я
  фільтрував і те, і те; тест `Явна_порожнеча_повертається_окремою_ознакою`
  цю помилку зловив.

**Порти, яких у пакеті не було** (додані, не змінені — див. `Q-032`):
`ITemplateVersionStore` (атомарний інкремент ревізії за `R-B7`),
`IRowStore` (версії рядків, створення, «дотик», ознаки осиротілості),
`IRecalculationJob` (маркер задачі).

### Стан Етапу 1

| Модуль | Тестів `Stage1` | Стан |
|---|---:|---|
| 1.1 `Ecr.Domain` — метадані | входять у 58 | ✅ |
| 1.2 `Ecr.Domain` — документи | входять у 58 | ✅ |
| 1.7–1.9 `Ecr.Application` | 24 | ✅ |
| 1.3 `Ecr.Infrastructure` — конвенції і 31 конфігурація | — | ✅ звірено з живою БД |
| 1.4 міграція + `07`, `08`, `09-seed` | — | ✅ застосовано і перевірено на живій БД |
| 1.4 seed і `SqlServerFixture` | — | ✅ фікстура піднімає базу наскрізь |
| 1.5 `ICellStore` — `NormalizedCellStore`, `BulkCellLoader` | 6 + 3 | ✅ зелені на реальній БД |
| 1.6 `IMetadataCache` — `MetadataCache` | 4 | ✅ зелені на реальній БД |
| 1.9 аудит — `11-audit-tables.sql`, `AuditWriter` | — | ✅ таблиць `aud.*` раніше не створював ніхто (`Q-049`) |
| DI-композиція — `AddEcrInfrastructure`, `AddEcrApplication` | — | ✅ застосунок стартує |
| 1.10 `Ecr.Api` — конвеєр, помилки, health, OpenAPI | 13 | ✅ перевірено запуском |
| 1.11 `tools/Ecr.DataGen` — генератор і `GateBenchmark` | — | 🟡 5 із 6 замірів гейта; №4 (архівація року) потребує стенду |
| Доробка: `SchemaValidator`, конкурентність, аудит, архітектурні правила | 42 | ✅ заглушок `Stage1` — **нуль** |
| 1.10 `Ecr.Api` | 19 | ⬜ потребує запущеного застосунку і БД |
| 1.11 `tools/Ecr.DataGen` | — | ⬜ |

**`Stage=Stage1&Category!=Integration`: 117 усього, 82 passed, 35 failed.**
Уся не-Integration частина, яку можна перевірити без бази, — зелена.

### Аудит Етапів 0–1 (`Q-028`, 2026-09-04)

Наскрізна перевірка скриптами і запуском, не переглядом. Підтверджено:

| Що | Факт |
|---|---|
| Чиста збірка `Debug` і `Release` | **0 errors** (937 попереджень, усі 13 класів — із `WarningsNotAsErrors`) |
| Тести | 486 не-Integration (27 passed) + 119 Integration + 23 frontend = **628**, 0 skipped |
| Цілісність тестів | 96 файлів, імена **збігаються з `06*` один в один**; `Skip` 0, `Ignore` 0; `Assert.Fail` 474 = 489 − 15 замінених у модулі 1.1 |
| Скелет проти `05*` | 302 секції, 251 побайтово; **усі 50 розбіжностей мають запис у журналі** |
| Контракт проти коду | **45 із 45** блоків із локатором збігаються побайтово |
| Дерево §1 проти диска | 253 файли, 0 відсутніх |
| Архітектурні межі | `Domain` без пакетів; `Application` без EF/Infrastructure/Adapters; на `Ecr.Api` не посилається ніхто |
| Контрактні типи | жодного `NotImplementedException` |

**Головне для планування:** `Q-027` (22 сутності проти схеми) не блокує
модуль 1.3 — відсутні цілком сутності лежать у `calc`, `dic`, `ext`, `sec`,
`wf`, `arc`.

> ⚠ **ВИПРАВЛЕНО 2026-09-04 (`Q-037`).** Тут раніше стояло «у `cfg.*` і
> `doc.*` розбіжностей нуль». Це було правдою лише про те, що порівнювалося:
> `Q-027` звіряв **склад сутностей**, а не стовпці. Порівняння з **реальною
> базою** після застосування міграції дало **57** розбіжностей — довжини й
> точності, 12 відсутніх `DEFAULT`, усі 22 `CHECK`, обидві `SEQUENCE`,
> 4 тіньові FK-колонки, 4 винайдені EF індекси, дві хибні форми ключа.
> Усе виправлено; звірка з базою тепер дає нуль по стовпцях, індексах і
> `DEFAULT`. Урок: «зелена збірка + зелені не-Integration тести» не говорять
> про фізичну модель **нічого**.

Виправлено в аудиті: застарілий локатор `ParseResult` у `02b`; неузгоджене
число в `Q-027`.
Відкрито: `01-filegroups.sql` містить плейсхолдер `<path>` і як є не
виконується — рішення про шляхи розгортання за людиною.

### Аудит Етапів 0–1, п'ятий прохід (`Q-044`, 2026-09-04)

| Що | Факт |
|---|---|
| Збірка `Debug` і `Release` | **0 errors**, 930 warnings |
| Контракт проти коду | **39 із 39** блоків побайтово |
| Схема БД проти SQL-скриптів | **6 із 6** побайтово |
| Міграція EF проти схеми БД | 33 таблиці, **0 розбіжностей** |
| Імена тестів `06*` проти коду | **491 = 491**, зниклих 0 |
| Дерево §1 проти диска | 245 імен, 0 відсутніх |
| Коди помилок / права / ендпоінти | 42 = 42 · вигаданих прав 0 · 56 = 56 маршрутів |
| Посилання `ФВ`/`D`/`R` у коді | 182 унікальних, неіснуючих 0 |
| Архітектурні межі, U+FFFD, `NotImplementedException` у контракті | чисто |

Виправлено: розсинхрон `PeriodKey` із контрактом; `08-system-tables.sql` і
`09-seed.sql` дописані в §13, `08` — у порядок розгортання (без нього seed
падає); два нові тести і позначки `Integration` синхронізовані з `06c`.

`Q-027`: лишається **20** розбіжностей сутність↔схема (`calc` 9, `dic` 4,
`ext` 4, `sec` 1, `wf` 2). У `cfg`, `doc`, `uom` — нуль.

### Аудит Етапів 0–1, шостий прохід (`Q-048`, 2026-09-04)

Попередні п'ять проходів звіряли **форму** — що оголошене існує і збігається.
Цей перевіряв **поведінку**.

| Уперше перевірено | Результат |
|---|---|
| Синтаксис усіх скриптів, зокрема `03`–`05`, які ніколи не виконувалися | `PARSEONLY` — 10 із 10 чисті |
| Ідемпотентність кожного скрипта (двічі поспіль) | 9 із 10 → після `Q-047` **10 із 10** |
| Тригери незмінності в базі | **були відсутні** (`Q-045`) → 3 із 3, тести 7 із 7 |
| Партиційна архівація в дії | `TRUNCATE WITH (PARTITIONS)` звільнив 112 із 114, сусідні партиції недоторкані |
| Класи попереджень проти `WarningsNotAsErrors` | 11 фактичних, невідомих 0 |

**Три знахідки, усі виправлені:**

1. `Q-045` — тригери незмінності оголошені через `HasTrigger()`, але їх не
   створював **ніхто**: ця анотація лише вимикає `OUTPUT`-клаузу для EF.
   Третій випадок після `Q-035` і `Q-041`, і знову **без жодної помилки**:
   структурна зміна опублікованої версії проходила мовчки.
2. `Q-046` — сам тригер у контракті падав на `ISNULL(tinyint, -1)` з
   `Msg 220 Arithmetic overflow` і не перевіряв нічого. Причому лише на
   опублікованих версіях — там, де ціна помилки найвища.
3. `Q-047` — `02-partitions.sql` єдиний із десяти не переживав повторного
   запуску.

---

### Завершення Етапу 1 (`Q-049`…`Q-056`, 2026-09-04)

Застосунок **стартує і відповідає** — перевірено запуском, не збіркою:

```
/health/live       200
/health/db         200 Healthy — edition, effectiveMode, rcsi, filegroups,
                   partitionsAhead 15, limitations
/health/ready      200 Degraded (jobs і sources — Етап 5)
/api/v1/templates  401, а не редирект: це API (ФВ-6.1)
/openapi/v1.json   200, 106 КБ
```

**Чотири блокери, знайдені під час роботи, — усі одного класу:** об'єкт
описаний у схемі, згаданий у коді, і не створюється нічим.

| | Що не створювалося | Наслідок |
|---|---|---|
| `Q-035` | партиційні схеми на таблицях | архівація мовчки не звільняє місце |
| `Q-041` | таблиці `sys_ecr` | seed падає на першому `MERGE` |
| `Q-045` | тригери незмінності | структуру опублікованої версії можна змінити |
| `Q-049` | таблиці `aud.*` | модуль 1.9 нездійсненний |

**Ще три дефекти, які виявив лише запуск:**

* `Q-052` — `DATABASEPROPERTYEX(…,'IsReadCommittedSnapshotOn')` **не існує** і
  повертає `NULL`: health повідомляв би про вимкнений RCSI на кожному
  розгортанні. Моніторинг, який завжди червоний, перестають читати.
* `Q-054` — `AddNegotiate()` виконується на **кожен** запит і вимагає
  `IConnectionItemsFeature`: без Kestrel/HTTP.sys 500 отримує навіть
  `/health/live`.
* `Q-055` — фізичні імена файлів у `01-filegroups.sql` не містили імені бази,
  тобто **дві бази ECR на одному інстансі були неможливі**.

---

### Definition of Done Етапу 1

| Пункт | Стан |
|---|---|
| Публікація робить структуру незмінною; тригер відхиляє структурний `UPDATE` | ✅ `Q-045`, `Q-046`, 7 тестів |
| `PresentationRevision` інкрементується одним statement із `OUTPUT` | ✅ перевірено лічильником походів до сервера |
| batch-PATCH: конфлікт відхиляє **весь** батч | ✅ `ConcurrencyTests` |
| `TableRow.ModifiedAt` піднімається при зміні комірок | ✅ ключовий тест конкурентності |
| Три стани комірки розрізняються | ✅ `CellStoreTests` |
| Порожні комірки не матеріалізуються | ✅ |
| `SEQUENCE` дає `Id` до вставки; `SqlBulkCopy` одним проходом | ✅ |
| Аудит пишеться **пакетно** | ✅ 100 комірок = 1 похід до сервера |
| Seed виконується ідемпотентно | ✅ |
| `/health/ready` і `/health/db` відповідають; `/health/db` показує режим редакції | ✅ |
| **Архітектурні тести зелені (8 правил)** | ✅ `LayerRulesTests` 8 із 8 |
| Усі тести `Stage=Stage1` зелені | ✅ заглушок `Stage1` — нуль |

### Доробка Етапу 1 (`Q-057`…`Q-059`, 2026-09-04)

Реалізовано 42 заглушки: `SchemaValidator` 8, конкурентність 6, аудит 5,
архітектурні правила 8, заборонені API 6, цілісність контракту і ліцензії 7,
ревізія та `UnitOfWork` 6, контролери й конвенції 6, `E20` 1.

**`Q-058`: правило «час лише через `IClock`», щойно написане, знайшло чотири
порушення у щойно написаному мною коді** — `RowStore`, `SchemaValidator`,
`DatabaseHealthCheck`, `StartupSequence`. Усі виправлені. Висновок дорожчий за
виправлення: правило, яке не перевіряється автоматично, не діє.

**Чотири хибні спрацювання власних перевірок** — усі одного типу: правило
ловило текст, а не код (`.Result` у `ResultDiffJson`, `IExternalDataSink` і
`ALTER DATABASE` у пояснювальних коментарях, `PackageReference` у коментарі
«тут заборонений»). Плюс два, де хибним було саме правило: `ToList()` над
списком у пам'яті і `IBackgroundJob` із сімома реалізаціями за побудовою.
Кожне виправлене, а не приглушене: перше ж хибне спрацювання перетворює
архітектурний тест на шум, який вимикають.

---

### Стан тестів на кінець Етапу 1

| | Кількість |
|---|---:|
| Усього тестових методів | 491 |
| Реалізовано | 166 |
| Заглушок `Assert.Fail("not implemented")` | 325 |
| З них помічені `Stage1` | **0** |
| Проходить (з урахуванням `[Theory]`-випадків) | 184 |

Усі 361 падіння — це `Assert.Fail("not implemented")`. Інших винятків **0**.
Розподіл заглушок: `Stage2` 114, `Stage3` 108, `Stage4` 79, `Stage5` 22.

Заглушки за етапами: `Stage2` 114, `Stage3` 108, `Stage4` 79, `Stage5` 22,
`Stage1` 64, без позначки етапу 23.
`NotImplementedException` у `src`: 223 (`Ecr.Api` 68, `Ecr.Expressions` 37,
`Ecr.Infrastructure` 37, `Ecr.Application` 31, `Ecr.Domain` 22,
`Ecr.Calculations` 12, адаптери 16).

---

---

## Етап 2 — Вирази, валідація, перерахунок

---

## Етап 3 — Безпека, періоди, робочий процес

---

## Етап 4 — Реєстри, одиниці, розрахунки

---

## Етап 5 — Інтеграція, звітність, Excel, фонові задачі

---

## Етап 6 — Frontend

---

## Журнал тегів

| Тег | Дата | Build | Тести | Коментар |
|---|---|---|---|---|
| `stage-0` | 2026-09-04 | — | — | 344 файли створені, ще не збиралося |
| `stage-0-fixes` | 2026-09-04 | FAILED | — | BOOTSTRAP-FIX Q-002…Q-012; заблоковано Q-013, Q-014 |
| `stage-0-fixes-2` | 2026-09-04 | FAILED | — | Q-013 (A), Q-019, Q-020; лишалися Q-014, Q-018 |
| `stage-0-verified` | 2026-09-04 | OK | 628 знайдено, 628 failed | Q-014 (чернетки), Q-018 (B); Debug і Release зелені. ⚠ повідомлення коміту хибно каже «тести зелені» — для Етапу 0 зелений тест був би провалом |
| `stage-0-reviewed` | 2026-09-04 | OK | 628 знайдено, 628 failed, 0 passed | рев'ю з чистим контекстом: 8 правок у журналі, `Q-025`; відкрито `Q-026` |
| `stage-0-complete` | 2026-09-04 | OK | 628 знайдено, 628 failed | закрито `Q-014`, `Q-015`, `Q-018`, `Q-026`; відкрито `Q-027`. Дерево повне: 41 файл дописано |
| `stage-1` | | | | |
| `stage-1-reviewed` | | | | |
| `stage-2` | | | | |
| `stage-2-reviewed` | | | | |
| `stage-3` | | | | |
| `stage-3-reviewed` | | | | |
| `stage-4` | | | | |
| `stage-4-reviewed` | | | | |
| `stage-5` | | | | |
| `stage-5-reviewed` | | | | |
| `stage-6` | | | | |
| `stage-6-reviewed` | | | | |
