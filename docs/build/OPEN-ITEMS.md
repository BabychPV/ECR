# Відкриті питання

**Що це.** Єдиний перелік того, що ще не закрито. Журнал
`questions/Q-*.md` веде історію кожного питання, але відповісти на
«що лишилось?» ним не можна: 290 записів із 300 мають статус `RESOLVED`,
і знайти серед них десять відкритих можна лише перебором.

**Як підтримувати.** Джерело істини — поле `status:` у `questions/Q-*.md`.
Перелік нижче зібраний з нього станом на **2026-09-17** і при зміні
статусів його треба перезібрати. Автоматичного генератора для цього файлу
поки немає — на відміну від `questions.md`, який будує
`tools/journal/regen-index.ps1`.

**Чого тут НЕМАЄ.** Записи зі статусом `RESOLVED`, чий власний підсумок
каже «частково» — див. §3. Бінарний перелік статусів не вміє висловити
«зроблено, крім рішення людини», і саме через це журнал не відповідає на
питання про залишок роботи.

---

## 1. Відкриті записи журналу (11)

### Заблоковані обладнанням

| ID | Суть |
|---|---|
| `Q-063` | Гейт `BR-07` не міряний: SQL Express непридатний за побудовою |
| `Q-145` | Гейт `BR-07` не пройдено на машині розробника; топ очікувань займали фонові черги, а не діагноз |

`Q-190` заблоковано транзитивно через `Q-063`: режим зберігання `Hybrid`
не можна прибрати, доки `BR-07` не виміряний на придатному обладнанні.

### Потребують рішення замовника

| ID | Суть |
|---|---|
| ~~`Q-153`~~ | ~~Чи має вбудована роль `Approver` заводити описи звітів. Сьогодні НЕ має: право небезпечне й у шаблон `Report.%` не потрапляє. Хто отримує `Report.EditDefinition` на майданчику — вирішує NCOC~~ |
| `Q-154` | `ФВ-10.7` називає **сім** державних форм, seed заводить одну (`IEC`). Решту шість треба описати реальними колонками, а не копією п'яти технічних |

✎ **2026-09-25: `Q-153` RESOLVED, розбіжність виявлена при аудиті.** Рішення
вже реалізоване й задокументоване в коді як `Q15-07`: «окреме право,
ВИДАЄТЬСЯ ЯВНО» — `src/Ecr.Infrastructure/Persistence/Sql/09-seed.sql:68-84`.
`Report.EditDefinition` заведено `IsDangerous = 1`, а шаблон `Report.%` для
складеної ролі `Approver` бере лише права з `IsDangerous = 0`, тож це право
в набір `Approver` не потрапляє; видає його явно адміністратор (журналюється
`DangerousPermissionsGranted`). Запис лишався `OPEN` уже після того, як
рішення реалізували — перекреслено, не видалено.

### Мертвий код, що чекає архітектурного рішення

| ID | Суть |
|---|---|
| `Q-190` | `TableDef.SwitchStorage` / `CellStorageMode.Hybrid`: `StorageMode` вставляється як `Normalized` у конструкторі й ніде не читається умовно; `SwitchStorage` не має жодного викликача |
| ~~`Q-192`~~ | ~~`ITemplateStructure` / `CachedTemplateStructure`: порт існував лише заради синхронного читання кешу для `ExtractDependencies`~~ ✎ **2026-09-22: закрито, запис застарів.** Обидва файли прибрані комітом `33a02ff8` (`#346`, `AR-06`) задовго до цього рядка — лишався `OPEN` уже після того, як прогалину закрили |

### Відсутня поверхня API, яка вже специфікована

| ID | Суть |
|---|---|
| ~~`Q-194`~~ | ~~`DeleteRegistryEntryHandler` існує… але не має жодного `[HttpDelete]`~~ ✎ **2026-09-21: закрито, запис застарів.** Маршрут є (`RegistriesController.cs:240`), споживач у клієнті й тести API теж; прийшло з `BE-01` (#361). Рядок лишався `OPEN` і встиг послати агента закривати те, чого немає |
| `Q-195` | Немає ендпоінта, що показав би стан затвердження всіх аркушів документа за період чи історію поданих зрізів, хоча `IWorkflowStore.GetSheetsAsync`/`GetSnapshotsAsync` пишуть ці дані й не мають викликачів |

### Робота, почата зрізом і не завершена

| ID | Суть |
|---|---|
| `Q-341` | Подробиця відмови (`Detail`) їде клієнту готовим українським реченням: 316 кидків 4xx без `messageKey` у 95 файлах проти 16 із ключем, при тому що мови продукту — `en`/`ru`/`kz`. Зроблено перший зріз (12 відмов `PatchCellsHandler` — кожне збереження комірки) і заведено храповик `MessageKeyRatchetTests` проти `contracts/localization-debt.md`, який не дає розриву рости. Решта 95 файлів лишається; `NotFoundException` не має куди покласти ключ — у типу немає `Details` |

**Конструктор звітів у застосунку (`D-52a`, рішення замовника 2026-09-19) — в
роботі.** Запису журналу `Q-*` немає: це не питання, а обсяг, повернутий
рішенням. Зроблено перший крок: визначення звіту керує побудовою зрізу
(колонки й порядок — з `ColumnsJson`, невідома колонка відмовляє при створенні
версії) і `GET /api/v1/reports/snapshots/{id}/rows` зі споживачем на сторінці
зрізів. Другий крок (#445): діалект виразів `Report` — рушій і валідація.
Лишається, по одному PR: `RulesJson` schema 2 і застосування правил «умова →
значення / приховати рядок»; макет з однією групою й підсумками; параметри звіту;
вивантаження XLSX; назви колонок мовами каталогу. Межу «не робимо» тримає
`D-52a`; `D-53` чинне — у `rpt.*` лише адитивні зміни.

### Міграція даних замовника

Утиліти `tools/Ecr.Bootstrap.Excel` і `tools/Ecr.Migration.PiAf` мають
точки входу, що кидають `NotImplementedException` зі специфікацією.
⚠ Це НЕ «з нуля»: будівельні блоки (адаптери Excel і PI AF, ~2 100 рядків
робочого коду з тестами) уже є, а найтонше місце — перенесення дат
чинності (`LegacyValidityImport`) — вирішене й покрите тестами.

Перший крок, що НЕ залежить від замовника: `LegacyRowSerializer` — чиста
функція, яка задає критерій приймання для всього іншого (побайтна звірка
відтвореного `;`-рядка з оригіналом).

⚠ Тут раніше стояло «схема `ext.*` не створюється розгортанням» — це було
неправдою: таблиці `ext.Legacy*` створюються міграціями EF і в розгорнутій
базі присутні. Розбір помилки — у `MIGRATION-ESTIMATE.md` §3.1.

Повний розбір, етапи й перелік того, чого не можна зробити без даних
замовника — `MIGRATION-ESTIMATE.md`.

### Розбіжність документа й коду

| ID | Суть |
|---|---|
| `Q-149` | Провал фонового перерахунку не видно ніде, крім `itg.JobProgress`: клієнт отримав `200`, каскад формул не порахувався |
| ~~`Q-208`~~ | ~~`02a-db-schema.md` §17 описує 15 рядків під іменами `auth.*`, код використовує `login.*`~~ ✎ **2026-09-22: закрито, запис застарів.** §17 переписано на `login.*` і перекваліфіковано на «ілюстрацію» комітом `dd312f9f` (`#369`, 2026-09-19); сторож `SeedIllustrationTests` не дає повтору |

---

## 2. Провалена вимога

~~**`BR-07` — гейт продуктивності: 3 з 6 критеріїв не виконано.** Усі три
про конкурентність в один розділ: 63 RPS проти цільових 122.5; 48 207 зі
112 500 запитів не обслужено; агрегація 1565 мс проти 500.~~

✎ **2026-09-20: переміряно, числа вище застаріли — не виконано 1 критерій, і
1 не виміряно.** Замір на `main = 2012736`, SQL Server 2025 Developer (не
Express), 108.9 млн рядків,
`tools\br07-load-test.ps1 -Server localhost -Database EcrBr07Base`:

| Критерій | Ціль | Факт | |
|---|---|---|---|
| `ReadSliceAsync` 500×60, p95 | < 600 мс | 166.6 мс | ✔ |
| `ApplyAsync` 100 комірок, p95 | < 150 мс | 8.3 мс | ✔ |
| Агрегація періоду, p95 | < 500 мс | **562.4 мс** | ✘ |
| 125 RPS × 900 с | ≥ 122.5, 0 необслужених | 125.0, 0 | ✔ |
| Розмір на рік | — | ≈ 3.2 ГБ | ✔ |
| Архівація | — | **не виміряно** | — |

⚠ Одна машина: генератор, SQL Server і (в HTTP-прогоні) `Ecr.Api` ділять ті
самі 12 логічних ядер — затримки це верхня межа, темп нижня. Порівняння
«до/після» на цьому стенді правомірне, перенесення на обладнання замовника —
ні, тож `Q-063` / `Q-145` лишаються відкритими. Прогін один; розкид не знято.
База `EcrBr07Base` збережена — перезняти без наповнення: `-SkipGenerate`.

⚠ Це базова лінія «до»: рішення замовника 2026-09-19 «усюди 16 знаків після
коми» змінить тип `ValueNumeric`, тобто розмір рядка й скан агрегації —
рівно той критерій, що не пройшов. Після міграції переміряти тим самим
рядком.

Рішення `D-21` (компроміс режиму зберігання) за замовником.

---

## 3. Записи `RESOLVED`, чий власний підсумок каже «частково»

Сім записів закриті статусом, але їхній фінальний рядок містить застереження:
`Q-006`, `Q-202`, `Q-204`, `Q-206`, `Q-210`, `Q-222`, `Q-230`.

Причина системна: перелік статусів має рівно два значення (`RESOLVED` /
`OPEN`, що стережеться `JournalIntegrityTests`), і висловити «зроблено, крім
рішення людини» ним неможливо. **Пропозиція:** додати `PARTIAL` і поле
`blocked-on:` до переліку та до сторожа.

Найгостріший із семи — `Q-210`: за статусом закритий, фактично містить
юридичну прогалину, див. `CUSTOMER-QUESTIONS.md`.

---

## 4. Що робити з цим файлом

Поки немає генератора, перевірити актуальність можна так:

```powershell
Get-ChildItem docs/build/questions -Filter 'Q-*.md' |
  Where-Object { (Get-Content $_.FullName -TotalCount 12) -match '^status:\s*OPEN' } |
  Select-Object BaseName
```

Якщо результат розходиться з §1 — перезберіть розділ.

Суміжні документи: `problems.md` (реєстр дефектів),
`unreachable-mechanisms.md` (механізми без шляху в UI; за станом на
2026-09-17 частково застарів в оптимістичний бік),
`CUSTOMER-QUESTIONS.md` (питання, на які відповідає замовник).
