# Прохід інтерфейсом «як користувач» — 2026-09-23

**Стенд:** жива база `EcrUx` (`tools/setup-dev-db.ps1`, повний профіль
файлових груп), API `http://localhost:5080`, Vite `http://localhost:5173`,
проєкт `P99819007`, період `202609` (Open), документ `DOC-000001` (91
таблиця, ~90 % комірок заповнено генератором).

**Ролі:** `ux-admin` (усі **41** права каталогу, грант `Manage` на проєкт) і
`ux-operator` (`Document.View/Create/Export/Import`, грант `Write`) — щоб
бачити не лише «екран малюється», а й що оператор **не бачить** зайвого.

**Чим пройдено:** Chromium 1440×900 у світлій і темній темі + ручні
взаємодії (правка комірки, перевірка, подання, діалоги створення). Кожне
твердження нижче стоїть на знімку або на заміряному значенні, не на
враженні; де я перевіряв здогад і він не підтвердився — так і написано.

---

## ⚠ Про межі цього звіту

- **Дві знахідки я зняв сам, перевіривши.** (1) «Набране `4242` доїжджає в
  комірку як `4`» — це артефакт швидкості автомата: із затримкою 150 мс між
  клавішами доїжджає `4242` повністю. (2) «Символ-заміна `�` у тексті
  попередження на екрані виразів» — це кодування моєї консолі, на знімку
  там правильне тире.
- Знахідки попереднього проходу (`UI-WALKTHROUGH.md`, 2026-09-18)
  перевірені повторно. **Виправлено:** `F2` (панель розрахунків більше не
  монтується оператору), `F3` (посилання на документ несе `periodKey`),
  `F4` (неіснуючий документ пояснює себе реченням, а не «HTTP 404»),
  `F7` («Missing filegroups» показує «—»), `F9` (оператор не бачить
  «Submit»), `F10` (каталог текстів сторінками: 101 рядок і 3 681 px
  замість 1 340 і 50 730). `F1` — див. **U-01** нижче: українську з екрана
  прибрали, але речення замість неї не з'явилося.

---

## Перелік знахідок

| ID | Екран | Суть | Вага |
|---|---|---|---|
| U-01 | Вхід | Хибний пароль — жодного слова про те, що сталося | псує враження |
| U-02 | Сітка документа | Відмова типу комірки — українською на англійському екрані | псує враження |
| U-03 | Перелік документів | Колонка «State» каже «—» там, де смуга над нею каже «1 Draft» | псує враження |
| U-04 | Сітка документа | У шапки сітки немає ні заливки, ні лінії — вона не читається як шапка | псує враження |
| U-05 | Сітка документа | Числа вирівняні ліворуч і подані трьома різними форматами в одній таблиці | псує враження |
| U-06 | Документ | Дріб «0 / 91» без підпису читається як «нічого не введено» | псує враження |
| U-07 | Редактор виразів | Порожній редактор одразу показує червону помилку | псує враження |
| U-08 | Довідники | Два взаємно суперечливі порожні стани один під одним | дрібниця |
| U-09 | Завдання, Джерела | Два й три порожні стани на одному екрані, фільтри між ними | дрібниця |
| U-10 | Періоди, Документи | Єдиний доступний варіант не обрано — екран відкривається порожнім | дрібниця |
| U-11 | Безпека | Заголовки колонок — сирі коди прав (41 шт.) | псує враження |
| U-12 | Безпека | Заливка шапки збігається із заливкою смугастих рядків | дрібниця |
| U-13 | Діалог проєкту | З п'яти обов'язкових полів зірочку має одне | дрібниця |
| U-14 | Стан системи | Картки названі внутрішніми кодами перевірок | дрібниця |
| U-15 | Документ | Перемикач формату стоїть перед «Export», а читається як частина «Import» | дрібниця |
| U-16 | Сітка документа | Правильна правка зберігається сама, кнопка «Save» рахує лише відхилені | псує враження |
| U-17 | Сітка документа | Та сама відмова надрукована двічі поруч | дрібниця |

---

### U-01 · Хибний пароль: екран не каже, що сталося

- **Екран:** `/login`.
- **Кроки:** `ux-admin` + завідомо хибний пароль → «Sign in».
- **Очікувано:** речення, яке називає причину.
- **Фактично:** червона рамка із заголовком «**Sign in to continue.**» —
  тобто вказівкою, а не повідомленням про відмову, — і під ним самі лише
  `ECR-AUTH-0401` і кореляція. Жодного слова про пароль.

**Чому так.** Сервер надсилає `detail` = «Невірне ім'я користувача або
пароль.» українською й **без** `messageKey`. За рішенням 2026-09-20
(`shared/ui/ErrorAlert.tsx`) подробиця без `messageKey` не показується
взагалі. Тобто українську з екрана прибрали правильно, а речення мовою
користувача на її місце не поставили.

**Що зробити.** `messageKey` на відмову входу + рядок каталогу. Це один із
316 кидків, які рахує `Q-341`, але цей — **перший екран продукту**, і його
варто закрити поза чергою.

### U-02 · Відмова типу комірки — українською на англійському екрані

- **Екран:** `/documents/1`, сітка.
- **Кроки:** подвійний клік у комірку числової колонки `C5` → набрати
  `abc` → Enter.
- **Очікувано:** пояснення мовою інтерфейсу.
- **Фактично:** «**Колонка «C5» очікує число.**» — українською, під
  англійським заголовком «Not saved — see the error above».
- **Знімок:** `cell-text-into-number.png`.

**Джерело:** `Ecr.Application/Documents/CellValueReader.cs:190` —
`Mismatch(...)` збирає текст рядком у коді, `messageKey` немає.

⚠ Це не «ще один з 316». `Q-341` каже, що перший зріз закрив «12 відмов
`PatchCellsHandler` — кожне збереження комірки». Ця відмова приходить саме
із збереження комірки, але живе в `CellValueReader`, і зріз її **не
дістав**. Тобто найчастіша інтерактивна помилка продукту — досі сирим
українським реченням.

### U-03 · «State» каже «—» там, де смуга каже «1 Draft»

- **Екран:** `/?periodKey=202609`.
- **Кроки:** відкрити перелік із періодом стенда.
- **Очікувано:** одна відповідь про стан документа на одному екрані.
- **Фактично, одночасно:** смуга над таблицею — «**1** Draft»; фільтр
  `State = Draft` повертає цей самий рядок; а колонка «State» у ньому —
  «**—**».

**Чому так.** Правило «аркуш складу без рядка стану — **чернетка**»
записане й виконане у двох місцях
(`DocumentStore.WhereState`, `DocumentListSummaryStore`) і **не** виконане
в третьому: `DocumentStore.StatesBatchAsync` віддає лише ті аркуші, для
яких є рядок у `doc.ApprovalState`, а решта падає в «стану не знаємо».
Перевірено відповіддю API: `GET /api/v1/documents?periodKey=202609` →
`"sheetStates":{}`, `GET /api/v1/documents/summary?periodKey=202609` →
`{"draft":1,...}`.

### U-04 · Шапка сітки не читається як шапка

- **Екран:** `/documents/1`, кожна з 91 таблиці.
- **Очікувано:** те саме, що вже роблять усі звичайні таблиці застосунку.
- **Фактично (заміряно):** `.rgHeaderCell` — `background-color:
  rgba(0,0,0,0)`, `border-bottom: 0px none`. Від даних її відрізняє лише
  `font-weight: 600`. Для порівняння, `<th>` на `/`, `/admin/units`,
  `/admin/templates` — `rgb(246,247,250)` і `font-weight: 700`.
- **Знімки:** `admin-20-document.png` (світла), `dark-20-document.png`.

⚠ Сітка **вміє** заливати комірку шапки: щойно колонку обрано, її заголовок
отримує заливку (`flow-07-cell-selected.png`). Тобто бракує саме стану
спокою, а не механізму.

### U-05 · Числа ліворуч і в трьох форматах одночасно

- **Екран:** `/documents/1`, «Table 1», один і той самий рядок.
- **Фактично, поруч:** `132.224500000…` (обрізано), `4,242`, `777.25`,
  `798.2309`; рядок «Total» — `8,929.8995`.
- **Заміряно:** `.rgCell { text-align: left }`; комірка `C1`
  `scrollWidth 176px` проти `clientWidth 140px` — тобто значення видно не
  повністю, і ознака цього — три крапки, а не заокруглення.

Три різні подачі числа в одній таблиці: сире значення зі складу (16
знаків), звичайне, і з роздільником тисяч (після правки і в підсумку).
Ліве вирівнювання додає своє: порядок величини в стовпці не читається.

⚠ Чого я **не** стверджую: що `132.2245000000000000` — дефект застосунку.
Колонка `C1` у даних генератора оголошена `String`, тож форматування
числа до неї не застосовується за побудовою. Дефект, який лишається
незалежно від даних, — **вирівнювання** і **розбіжність форматів між
даними, правкою і підсумком**.

### U-06 · «0 / 91» без підпису

- **Екран:** `/documents/1`, під перемикачем періоду.
- **Фактично:** сам дріб, без жодного слова. Означає «скільки таблиць
  заповнено **повністю**»: перевірено відповіддю
  `/tables/status` — таблиця 1 має `filledCells 710` із `inputCells 774`,
  отже в «заповнені» не потрапляє жодна.
- **Наслідок:** на документі, заповненому приблизно на 90 %, головний
  показник заповненості читається як «не введено нічого».

Підпису немає **навмисно** (`SheetFillSummary.tsx`, коментар): новий ключ
означав би правку `09-seed.sql`, яку тоді тримав інший PR. Причина
відпала — борг лишився.

### U-07 · Порожній редактор виразів одразу червоний

- **Екран:** `/admin/expressions`.
- **Кроки:** просто відкрити.
- **Очікувано:** порожній редактор нічого не порушує; помилка — після
  введення.
- **Фактично:** одразу під полем — червоний бейдж «**FINDINGS: 1**» і
  рядок «`ECR-TMPL-0422` Unexpected token ""». Користувач ще нічого не
  набрав.
- **Знімок:** `admin-05-expressions.png`.

### U-08 · Довідники: два порожні стани, що суперечать один одному

- **Екран:** `/admin/registries` на чистій базі.
- **Фактично, один під одним:** «**No registries yet** — Registries hold
  the reference lists columns pick from…» і нижче «**Pick a registry** —
  Pick a registry above to see its entries and validity windows.»
- **Чому це вада:** друга порада нездійсненна саме тоді, коли показана
  перша. Обирати нема з чого.
- **Знімок:** `admin-03-registries.png`.

### U-09 · Завдання і Джерела: порожні стани стосами

- **`/admin/jobs`:** «Enter a job id …» → прапорець «Only my jobs» →
  «**No jobs yet.**». Фільтр затиснуто між двома порожніми станами, і
  екран читається як зламаний.
- **`/admin/sources`:** три порожні стани поспіль («No collection sources
  configured», «No connections configured», «No collection runs»), причому
  в першого розділу немає ні власного заголовка, ні дії — на відміну від
  «Connections», у якого є «New connection».
- Дрібниця поруч: «No jobs yet**.**» з крапкою проти «No registries yet»,
  «Pick a project», «Enter a job id» без неї.
- **Знімки:** `admin-11-jobs.png`, `admin-09-sources.png`.

### U-10 · Єдиний варіант не обрано

- **`/admin/periods`:** відкривається на «Pick a project», хоча проєкт у
  системі **один**. Поки його не оберуть — порожній екран.
- **`/` (перелік документів):** поле «Period» порожнє, тому колонка
  «State» — «—» в усіх рядках і під нею підказка «Choose a period first…»,
  хоча відкритий період у проєкту рівно один і система його знає
  (`GET /projects/{id}/periods` → `202609 Open`).
- **Знімки:** `admin-08-periods.png`, `admin-01-documents.png`.

### U-11 · Заголовки колонок матриці прав — сирі коди

- **Екран:** `/admin/security`, вкладка «Roles».
- **Фактично:** 41 колонка з підписами `Calculation.EditConstant`,
  `Calculation.ManageRequiredInputs`, `System.ManageLocalization`, …
- **Чому це вада:** це той самий клас «код сервера як текст на екрані»,
  який за останній тиждень закривали щонайменше чотири рази (#437, #438,
  #440, #441). Тут він лишився в найширшому місці продукту.
- **Знімок:** `admin-07-security.png`.

### U-12 · Шапка матриці прав зливається зі смугастими рядками

- **Заміряно на тому ж екрані:** `<th>` — `rgb(246,247,250)`, перший
  `<td>` — **той самий** `rgb(246,247,250)`; нижньої межі в шапки немає.
  Тобто перший рядок даних і шапка — одна суцільна пляма.
- Те саме в темній темі (`dark-07-security.png`).

### U-13 · Зірочку має одне обов'язкове поле з п'яти

- **Екран:** діалог «New project».
- **Фактично:** `*` стоїть лише біля «Site time zone (IANA)», а підпис
  унизу перелічує п'ять незаповнених: «Still needed: Code, Name, Site time
  zone (IANA), Template version, Period policy».
- **Знімок:** `flow-02-new-project-dialog-empty-submit.png`.

### U-14 · Картки стану названі кодами перевірок

- **Екран:** `/admin/health`.
- **Фактично:** заголовки карток — `db`, `jobs`, `sources` (малими,
  як ідентифікатори), хоча текст під ними вже людський («Database is
  available.», «The scheduler is running.»).
- **Знімок:** `admin-18-health.png`.

### U-15 · Перемикач формату стоїть не там, де діє

- **Екран:** `/documents/1`, панель дій.
- **Фактично, зліва направо:** `Validate` · `Import from Excel` ·
  `Excel CSV JSON` · `Export` · … Перемикач формату керує **експортом**,
  але стоїть одразу після «Import from Excel» і без власного підпису,
  тому читається як налаштування імпорту.
- **Знімок:** `admin-20-document.png`.

### U-16 · «Save» рахує лише те, що не збереглося

- **Екран:** `/documents/1`, сітка.
- **Фактично:** правильна правка після Enter зберігається **сама** — поруч
  з'являється «Saved / Recalculating…», а кнопка лишається «Save (**0**)»
  і вимкненою (`flow-09-cell-after-enter.png`). Кнопка набирає лічильник
  лише тоді, коли сервер правку **відхилив**: «Save (**1**)» поруч із
  «NOT SAVED» (`cell-text-into-number.png`).
- **Чому це вада:** підпис обіцяє «стільки змін чекає збереження», а
  насправді показує «стільки змін збереження не пройшли». Модель роботи
  (автозбереження) і головна кнопка панелі суперечать одна одній.

### U-17 · Та сама відмова надрукована двічі поруч

- **Екран:** той самий.
- **Фактично:** у рядку кнопок — «**NOT SAVED — SEE THE ERROR ABOVE**»
  великими червоними; безпосередньо під ним — рамка «Not saved — see the
  error above» і вже сам текст причини. Перший напис відсилає «вище» до
  того, що насправді **нижче**.

---

## Що перевірено і виявилося справним

Межа звіту: нижче лише те, за чим стоїть спостереження, а не «виглядало
нормально».

- **Відмови за правом.** Оператор на 17 адмін-маршрутах бачить не порожній
  екран і не 403 у консолі, а речення з **назвою потрібного права** й
  посиланням «Documents». Перевірено всі 17.
- **Неіснуючі адреси.** `/no-such-page-2026` — «Page not found · This
  address does not match any screen in this system»; `/documents/999999999`
  — «Document 999999999 was not found» плюс код і «Retry».
- **Правка комірки.** Набране людською швидкістю значення доїжджає
  повністю; Enter зберігає; Undo вмикається; помилковий тип відхиляється з
  позначкою на самій комірці (червоний кут + заливка), а не мовчки.
- **Темна тема.** Усі 22 екрани читаються; жодного світлого прямокутника,
  жодного невидимого тексту.
- **Календар періодів.** Стани `CLOSED / GRACE PERIOD / OPEN / NOT OPEN
  YET`, «Reopen period» лише там, де це має сенс, поточний період
  позначено.
- **Кнопки без доступного імені.** Шукав навмисно, на кожному екрані обох
  ролей — **жодної**.
- **Поля без мітки.** Жодного, крім технічної `textarea.ime-text-area`
  редактора виразів, яка має `aria-hidden="true"`.
- **Горизонтальне переповнення сторінки.** Немає на жодному з 22 екранів.
