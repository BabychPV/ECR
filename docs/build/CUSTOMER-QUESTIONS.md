# Питання до замовника

**Що це.** Факти, яких виконавець не знає і за правилами `CLAUDE.md` не
має права вигадувати: фірмова символіка, юридичні й договірні умови,
визначення колонок державних форм, параметри середовища замовника.
Скрізь, де такий факт був потрібен, у коді стоїть **нейтральний дефолт**,
який можна замінити без переробки — і саме ці місця перелічені нижче.

**Навіщо окремий файл.** Половина цих прогалин **невидима для фільтра
`status: OPEN`** у журналі: вони закриті як `RESOLVED`, бо з боку коду
роботу зроблено — лишилось підставити факт. Без цього переліку вони
знаходяться лише випадково.

Історія обговорення до 2026-09-17 — у `archive/questions-for-customer.md`.

---

## 1. Блокує поставку

### ✅ Текст ліцензії — закрито 2026-09-17

**Рішення людини: MIT, правовласник `Babych P.V`.**

`LICENSE.txt` і `installer/Ecr.Installer/License.rtf` містять повний текст
MIT із рядком `Copyright (c) 2026 Babych P.V`. Заглушки
`PLACEHOLDER — NOT A REAL LICENSE TEXT` і `[year]`/`[fullname]` прибрано.

Блокер поставки інсталятора знято.

⚠ Що **не** вирішено цим: `Strings.wxl` досі оголошує
`Manufacturer = "ECR"`. Це не ліцензія, а назва виробника в діалозі
встановлення й у переліку програм Windows — окреме рішення про брендування,
разом із логотипом і фірмовими кольорами (`C-8`).

Журнал: `Q-210`.

---

## 2. Блокує роботу в продуктиві

### `C-5` — доменний контур

Без відповіді більшість продуктивних користувачів не отримає прав:
вхід через Windows-автентифікацію потребує знання домену й груп.

### `C-3` — автентифікація PI Web API

Ціна помилки **асиметрична**: неправильно обраний спосіб рапортує успіх
і не приносить даних. Тобто збій буде виглядати як порожній, але
справний збір.

### `C-1`, `C-2` — рядок підключення і версія RTQP

Потрібні для розгортання. Рядок підключення навмисно відсутній в
`appsettings.json` (`D-11`, `ФВ-6.11`) і передається лише змінною
оточення `ECR_ConnectionStrings__Ecr`.

---

### ✎ 2026-09-25 — права DDL для `arc.usp_ArchiveYear` у PROD

Блокує лише **нічний автоматичний розклад** архівації (`ux/r4-jobs-import`,
комміт `59f83e50`) — читання архіву (F-13) уже влито й не залежить від
відповіді.

`arc.usp_ArchiveYear` (`src/Ecr.Infrastructure/Persistence/Sql/
03-archive-proc.sql`) робить `TRUNCATE TABLE ... WITH (PARTITIONS(...))` і
`ALTER TABLE ... DROP/ADD CONSTRAINT` в одній транзакції. `D-66`
(`docs/tz/10-decisions.md`) прямо каже: застосунок **не має прав DDL** у
PROD. У репозиторії немає ні підпису сертифікатом, ні `EXECUTE AS OWNER`,
ні GRANT-скрипта під цю процедуру (перевірено `grep` по всьому
`Sql/*.sql`). `14-agent-jobs.sql` свідомо **не** ставить цю процедуру в
SQL Agent («архівація року — людина, не завдання»), а `ArchiveJob.cs`
кличе її напряму через з'єднання застосунку.

**Питання:** як `arc.usp_ArchiveYear` насправді авторизований під
обліковим записом застосунку в PROD? Три можливості:
1. Права видані окремо поза репозиторієм (звужений GRANT саме під цю
   процедуру);
2. Підпис сертифікатом/крок `EXECUTE AS` ще не написаний;
3. Це ще ніколи не розгорталось проти по-справжньому замкненого логіна.

Від відповіді залежить, чи можна вмикати гілку `ux/r4-jobs-import`
(нічний `RecurringScheduleService`) взагалі, і якщо так — хто пише
GRANT/сертифікатний скрипт і куди він іде: у `Sql/*.sql` (під
`tools/verify-sql-scripts.ps1`) чи в `docs/admin/operations-runbook.md`
(DBA-процедура, не код).

---

## 3. Потребує рішення методолога або відповідальної особи

### `Q-154` — колонки шести державних форм

`ФВ-10.7` називає **сім** форм, seed заводить одну (`IEC`). Решту шість
описано копією п'яти технічних колонок — тобто заглушкою. Потрібні
реальні визначення колонок.

### `Q-153` — хто заводить описи звітів

Чи має вбудована роль `Approver` право `Report.EditDefinition`. Сьогодні
не має: право небезпечне й у шаблон `Report.%` не включене.

### `D-21` — компроміс режиму зберігання

Від нього залежить перевимірювання проваленого гейта `BR-07`
(див. `OPEN-ITEMS.md` §2).

### ~~Масштаб округлення при передачі~~ — ЗАКРИТО 2026-09-20

~~Чинна система документує 16 знаків, реалізація передає 6. Розбіжність
реальна і потребує рішення методолога, а не судження виконавця.~~

✎ Відповідь людини, дослівно: «має бути 16 знаків у звіті, це конфігурація
комірки». Реалізовано як `D-148` (`docs/tz/10-decisions.md` §1.16): масштаб
виходу — `cfg.ColumnDef.Scale` колонки-приймача, за її мовчання
`NumericPolicy.DefaultOutputScale = 16`. ⚠ Лишається залежність від міграції
типів колонок: `calc.CalculationResult.Value` досі `decimal(28,10)`.

---

## 4. Оформлення й комунікація

### `C-8` — фірмові кольори NCOC

⛔ **Три твердження нижче застаріли того ж дня, коли їх писали, і ввели в
оману.** 2026-09-18 я послався на цей запис і сказав у описі PR #316, що
фірмового кольору й знака немає, а використано «наявну тему». Неправда —
обидва є, і обидва за рішенням людини від 2026-09-17. Запис не видалено, а
перекреслено: слід того, як він обдурив, корисніший за чисту сторінку.

**Що є насправді** (перевірено `git grep`, не прочитанням):

- **колір** — `src/Ecr.Web/src/shared/theme/theme.ts`, кортеж `brand`:
  «Основний колір — **фірмовий**, рішення людини від 2026-09-17: глибокий
  синій із делікатним переливом у бузковий». Відтінок дрейфує навмисно з
  226° (`brand[9]`) до 260° (`brand[0]`); значення підібрані обчисленням, і
  кожна пара, яку Mantine справді виводить, перевірена на контраст;
- **знак** — `src/Ecr.Web/src/shared/ui/BrandMark.tsx`: монограма `E` з трьох
  смуг, тобто рядків звіту. Рішення людини від 2026-09-17, варіант «Б».
  Пропорції підібрані під НАЙМЕНШИЙ розмір: на 16 px смуги зливалися;
- **контраст у темній темі** — виправлено. `cellState.dirty.dark.bg` тепер
  справжній темний фон, а не інверсія світлого, і поріг стереже
  `theme/__tests__/contrast.test.ts` (38/38 зелених, перевірено 2026-09-18).

**Що лишається питанням до замовника:** чи має NCOC **власну** офіційну
палітру й логотип, які мають замінити наші. Якщо так — заміна коштує один
кортеж у `theme.ts` і один файл `BrandMark.tsx`, бо жодного літерала кольору
в компонентах немає (`D7-01`, `ФВ-14.11`).

⚠ Обмеження заміни лишається чинним і воно єдине: кольори станів комірки
зв'язані **виміряними порогами контрасту**. Довільна фірмова палітра може
зробити стани нерозрізнюваними — тому будь-яка заміна проходить через
`contrast.test.ts`, а не через око.

~~У `theme.ts` стоїть один нейтральний кортеж.~~ ~~Логотипа немає — у шапці
текстове `ECR`.~~

> ~~⚠ Станом на 2026-09-17 у темній темі контраст тексту в редагованій
> комірці становить **1.35 : 1** проти норми 4.5 : 1. Це треба виправити
> **до** підстановки фірмових кольорів, інакше дефект замаскується під
> вимогу замовника.~~

### `C-7` — термінолог

≈450 рядків інтерфейсу. Переклади `ru` і `kz` **відсутні повністю**;
показується англійський запасний варіант.

> ⚠ Окремо: повідомлення помилок сервера захардкоджені **українською** —
> мовою, якої немає серед підтримуваних (`en`/`ru`/`kz`). Це не питання
> до замовника, а дефект; згадано тут, щоб термінолог не отримав
> неповний обсяг.

### `C-4` — транспорт і правила сповіщень

~~`NotificationJob` агрегує події й **не надсилає** їх: невідомі ні
транспорт, ні отримувачі, ні правила.~~

✎ **2026-09-25: формулювання застаріло, механізм реалізовано.** Транспорт і
механізм правил уже є: `src/Ecr.Infrastructure/Jobs/NotificationJob.cs`
(коментарі рядки 21-37 — черга `itg.NotificationOutbox`, безпечна деградація
в `Pending`, коли відправника не налаштовано, а не мовчазний «успіх»),
`src/Ecr.Domain/Entities/Notifications/NotificationChannel.cs`
(`NotificationChannelKind`: `Smtp`, `TeamsWebhook`), правила — UI вже є
(`src/Ecr.Web/src/features/notifications/RulesMatrixPanel.tsx`). Відкриті
лише реальні реквізити підключення (SMTP host/port/пароль, URL вебхука
Teams) і перелік отримувачів на проді — це факт середовища замовника, не
механізм продукту.

### Збір за підпис інсталятора (WiX)

Потребує рішення про оплату й сертифікат.

### Обмежені в часі призначення ролей

Механізм є, політика застосування — ні.

---

## 5. Як користуватись

Відповідь на будь-яке питання звідси — це зміна **даних або
конфігурації**, а не переробка: усі місця навмисно залишені замінними.
Отримавши відповідь, оновіть цей файл і відповідний запис журналу.
