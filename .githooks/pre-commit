#!/bin/sh
# =====================================================================
#  pre-commit — ЗОНАЛЬНИЙ БАР'ЄР ЗНЯТО (2026-09-08, пряме рішення людини)
# =====================================================================
#  До цього рядка тут стояло ~250 рядків, які фізично не давали PK1
#  комітити код, а PK2 — торкатися docs/, contracts/, CLAUDE.md,
#  .githooks/, .github/, .claude/, scripts/. Причина зняття: межі,
#  задумані як безпека, на практиці ставали черговою ASK-і-чекай
#  зупинкою щоразу, коли робота торкалася межі каталогу (docs/build,
#  contracts/openapi.snapshot.json — обидва вже розширювались окремо
#  того самого дня, перш ніж стало ясно, що межу треба прибрати
#  цілком, а не латати вкотре).
#
#  ЩО ЦЕ ЗНАЧИТЬ: жоден шлях у репозиторії більше не заборонений цим
#  хуком нікому. PK2 пише де потрібно для роботи — код, тести, docs,
#  contracts, скрипти.
#
#  ЩО НЕ ЗНЯТО (див. .githooks/pre-push і .github/workflows/):
#   - пряме надсилання у main і force-push — базова гігієна git,
#     не зональний бар'єр;
#   - шість гейтів CI (build, test, honesty-guard, server, client,
#     a11y) — критерій приймання коду в main. Це не «механізм sync»,
#     а перевірка, що код працює; її зняття означало б повернутися до
#     стану, з якого почався весь проєкт: тести, що нічого не доводять.
#
#  Історію зональної логіки видно в git log цього файлу.
# ---------------------------------------------------------------------

exit 0
