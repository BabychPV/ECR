#!/bin/sh
# =====================================================================
#  commit-msg — вимогу формату [PK1|PK2][TYPE] знято (2026-09-08)
# =====================================================================
#  Разом із зональним бар'єром у pre-commit: тег вузла в повідомленні
#  комітa був частиною процесу, який зняла людина прямим рішенням.
#  Історію перевірки видно в git log цього файлу.
# ---------------------------------------------------------------------

exit 0
