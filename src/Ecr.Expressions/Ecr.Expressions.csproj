<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Ecr.Expressions</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Ecr.Domain\Ecr.Domain.csproj" />
  </ItemGroup>

  <!--
    ⛔ Посилання на `NCalcSync` прибрано: `using NCalc` не мав у `src/` жодного
    місця виклику. Обчислювач тут власний (`Evaluation/Evaluator.cs` поверх
    `IEvaluationArithmetic`), а NCalc згадується лише в поясненнях до
    `Legacy`-семантики. Мертве посилання коштувало дорожче за нуль: воно
    обіцяло, що числа рахує саме той пакет, який стоїть у чинній системі.

    ⚠ Живий NCalc 1.3.8 лишається рівно в одному місці — `tests/Ecr.Legacy.Probe`
    (вимірювальний стенд, у поставку не входить). Це інший пакет і інша версія.
  -->

</Project>
