-- =============================================================================
-- 15-cell-tvp.sql — табличні типи гарячого шляху запису (`WR-02`).
--
-- Навіщо. До цього `NormalizedCellStore` слав по 12 параметрів на комірку в
-- списку `VALUES`, а `AuditWriter` — по 12 на рядок журналу. Наслідків два, і
-- обидва виміряні:
--
--   1. ТЕКСТ запиту залежав від КІЛЬКОСТІ рядків у батчі, тож кожен розмір
--      чанка компілював власний план. Лінійка `MS-01` §3.2 після `WR-01`:
--      сім планів `MERGE doc.CellValue` у кеші — по одному на розмір батчу.
--   2. Стеля 2100 параметрів на запит змушувала різати батч на чанки по 100
--      комірок: вставка 500×60 = 30 000 комірок давала 300 ПОСЛІДОВНИХ
--      `MERGE` в одній транзакції (і ~200 `INSERT` аудиту чанками по 150).
--
-- Табличний параметр знімає обидва: текст запиту сталий незалежно від розміру
-- батчу (один план назавжди), а параметр рівно один (межа 2100 не діє).
--
-- ⚠ Скрипт, а не міграція EF, бо цих типів немає в моделі EF — рівно як
-- `aud.*` (`Q-049`) і `sys_ecr.*`. Перелік і порядок виконання — у
-- `setup-dev-db.ps1`, `deploy-ecr.ps1`, `verify-sql-scripts.ps1`,
-- `br07-load-test.ps1` і `SqlServerFixture.BuildSchemaAsync`; у перших трьох є
-- сторож «скрипт є в дереві, але не виконується».
--
-- ⚠ Йде ПІСЛЯ міграцій (схема `doc`) і ПІСЛЯ `11-audit-tables.sql` (схема
-- `aud`): обидві мають існувати на момент `CREATE TYPE`.
--
-- ⛔ Типи КОЛОНОК мусять збігатися з цільовими стовпцями точно. Інакше
-- SQL Server додає неявну конверсію — і план, заради сталості якого все це
-- робиться, знову залежить від того, що приїхало. Числа взяті з
-- `CellValueConfiguration.cs` (34,16 і `datetime2(3)`) і з
-- `11-audit-tables.sql`, а не «на око».
--
-- ⛔ ДВА винятки з цього правила, обидва свідомі, обидва названі нижче:
-- `ValueString`, `OldValue`, `NewValue` оголошені `nvarchar(max)`, хоча
-- стовпці — `nvarchar(1000)`. Причина — `DAT-03`, і вона детально розписана
-- біля кожного типу.
--
-- Ідемпотентний: усе під `IF TYPE_ID(...) IS NULL`.
--
-- ⚠ Табличний тип НЕ МОЖНА змінити (`ALTER TYPE` для табличних типів не
-- існує), а `DROP TYPE` не пройде, доки на тип посилається хоч одна процедура
-- чи функція. Змінити форму = `DROP` + `CREATE`. Сьогодні на типи не
-- посилається нічого, крім коду застосунку, тож ціна такої зміни — один блок
-- нижче.
--
-- ⛔ 2026-09-21, `D-148`: масштаб `ValueNumeric` розширено 10 → 16. Самої
-- правки літерала в `CREATE TYPE` БУЛО Б НЕ ДОСИТЬ: у базі, де тип уже є,
-- умова `IF TYPE_ID(...) IS NULL` не спрацьовує, тип лишається з масштабом 10,
-- а `SqlMetaData` з масштабом 16 (`NormalizedCellStore`) шле в нього повне
-- значення — і СУБД округлює його МОВЧКИ. Тому умова дивиться на ФОРМУ типу,
-- а не лише на його існування.
--
-- ⛔ 2026-09-21, пізніше того ж дня: precision розширено 28 → 34 (рішення
-- людини). Форму звіряє той самий блок — він дивиться і на `precision`, і на
-- `scale`, тож другий перехід не потребував нового механізму, лише інших
-- чисел. Причина переходу: при масштабі 16 precision 28 лишає 12 цілих
-- розрядів, а множник `MWh → 3 600 000 000` із каталогу одиниць виводить уже
-- 278 МВт·год за цю межу. Тут розбіжність НЕ тиха, на відміну від масштабу:
-- завелике значення відхиляється «Arithmetic overflow» — але відхиляється
-- воно законне.
-- =============================================================================

SET NOCOUNT ON;
SET XACT_ABORT ON;
GO

-- -----------------------------------------------------------------------------
-- doc.CellValueTvp — джерело `MERGE doc.CellValue`.
--
-- ⛔ PRIMARY KEY (PeriodKey, TableRowId, ColumnDefId) — не прикраса і не
-- «щоб було». Два наслідки, кожен потрібен:
--
--   1. Кластерний ключ ТОЧНО збігається з ключем `doc.CellValue`, тож рядки
--      приїжджають на сервер уже відсортованими в порядку цільового індексу.
--      Без цього `MERGE` на 30 000 рядків або сортує їх сам, або бере
--      по одному — на найгарячішій таблиці системи.
--   2. Дублікат адреси в батчі падає ГУЧНО (2627), а не тихо. Раніше дублікат
--      усередині чанка валив `MERGE` помилкою 8672 («attempted to UPDATE or
--      DELETE the same row more than once»), але дублікат, розведений МЕЖЕЮ
--      ЧАНКА, проходив як два послідовні записи — тобто поведінка залежала від
--      того, на якій позиції в батчі опинилася комірка. Тепер батч один, і
--      відповідь на дублікат одна.
--
-- ⛔ `ValueString nvarchar(max)`, хоча стовпець — `nvarchar(1000)`. Це НЕ
-- недогляд і НЕ розширення межі: межа лишається на стовпці, задовге значення
-- впирається в неї на вставці й падає помилкою усічення. Причина рівно
-- зворотна — `nvarchar(1000)` ТУТ повернув би `DAT-03`: `SqlMetaData` з
-- оголошеною довжиною ОБРІЗАЄ довше значення на клієнті (`SqlMetaData.Adjust`),
-- тобто СУБД отримала б законні 1000 символів і мовчки їх зберегла. Той самий
-- механізм, що вже одного разу зробив утрату даних тихою через
-- `SqlParameter.Size`, і те саме рішення, що в
-- `NormalizedCellStore.AddNullable(string)`: без стелі на клієнті — гучна
-- відмова від СУБД замість огризка в базі.
--
-- ⚠ На сталість плану це не впливає: текст запиту від значень не залежить
-- узагалі, а неявна конверсія `nvarchar(max)` → `nvarchar(1000)` відбувається
-- на вставці вже відібраних рядків, не в пошуку.
-- -----------------------------------------------------------------------------
-- Застарілий за ФОРМОЮ тип знімається, щоб `CREATE` нижче створив новий.
-- Перевірка йде по `sys.columns.precision`/`scale`, а не по даті чи версії:
-- вона описує саме ту розбіжність, що шкодить, і не залежить від того,
-- скільки разів скрипт уже виконувався. Обидва числа звіряються разом —
-- саме тому перехід 28 → 34 не потребував нового механізму.
IF TYPE_ID(N'doc.CellValueTvp') IS NOT NULL
   AND NOT EXISTS (
        SELECT 1
        FROM sys.table_types AS tt
        JOIN sys.columns     AS c ON c.object_id = tt.type_table_object_id
        WHERE tt.name = N'CellValueTvp'
          AND SCHEMA_NAME(tt.schema_id) = N'doc'
          AND c.name = N'ValueNumeric'
          AND c.precision = 34
          AND c.scale = 16)
    DROP TYPE doc.CellValueTvp;
GO

IF TYPE_ID(N'doc.CellValueTvp') IS NULL
    EXEC(N'
CREATE TYPE doc.CellValueTvp AS TABLE
(
    PeriodKey            int            NOT NULL,
    TableRowId           bigint         NOT NULL,
    ColumnDefId          int            NOT NULL,
    TableDefId           int            NOT NULL,
    ValueString          nvarchar(max)  NULL,
    ValueNumeric         decimal(34,16) NULL,
    ValueDate            datetime2(3)   NULL,
    ValueBool            bit            NULL,
    ValueRegistryEntryId int            NULL,
    ValueUnitId          int            NULL,
    IsCalculated         bit            NOT NULL,
    IsEmpty              bit            NOT NULL,
    PRIMARY KEY (PeriodKey, TableRowId, ColumnDefId)
);');
GO

-- -----------------------------------------------------------------------------
-- aud.CellChangeTvp — джерело `INSERT INTO aud.CellChange` (`WR-08`, частина
-- «запис через TVP у тій самій транзакції»).
--
-- ⛔ БЕЗ первинного ключа, і це не забудькуватість. Журнал аудиту — потік
-- подій, а не набір станів: та сама комірка може змінитися двічі в межах
-- одного батчу (правка й перерахунок), і обидва рядки мусять лягти. Ключ
-- (ChangedAt, TableRowId, ColumnDefId) відкинув би другий, тобто аудит
-- мовчки втрачав би зміни — рівно те, проти чого він існує.
--
-- ⚠ Ціна відсутності ключа тут нульова: `INSERT … SELECT` із табличного
-- параметра порядку не потребує, а `aud.CellChange` має власний кластерний
-- ключ (ChangedAt, Id) і власну схему партиціонування.
--
-- ⛔ `OldValue`/`NewValue` — `nvarchar(max)` із тієї ж причини, що
-- `ValueString` вище, і ціна помилки тут ВИЩА: комірку можна перечитати, а
-- неправдивий рядок аудиту нічим не спростуєш. Стовпці лишаються
-- `nvarchar(1000)`, задовге значення валить батч на сервері (2628) —
-- дослівно те, що описує `DAT-03`.
--
-- ⚠ `RowKey`/`Origin`/`CorrelationId`, навпаки, оголошені ТОЧНО як стовпці:
-- у них межу стереже код (`AuditWriter.EnsureFits`) ще до того, як значення
-- дійде до `SqlDataRecord`, тож тихого обрізання бути не може, а точний тип
-- знімає неявну конверсію.
-- -----------------------------------------------------------------------------
IF TYPE_ID(N'aud.CellChangeTvp') IS NULL
    EXEC(N'
CREATE TYPE aud.CellChangeTvp AS TABLE
(
    ChangedAt       datetime2(3)   NOT NULL,
    PeriodKey       int            NOT NULL,
    DocumentId      bigint         NOT NULL,
    TableRowId      bigint         NOT NULL,
    RowKey          nvarchar(100)  NOT NULL,
    ColumnDefId     int            NOT NULL,
    OldValue        nvarchar(max)  NULL,
    NewValue        nvarchar(max)  NULL,
    ChangedByUserId int            NOT NULL,
    Origin          nvarchar(32)   NOT NULL,
    IsLateEdit      bit            NOT NULL,
    CorrelationId   nvarchar(64)   NULL
);');
GO
