/*
 * ⚠ ЗАГЛУШКА згенерованого файла.
 *
 * Справжній вміст створює `npm run api:types`
 * (`openapi-typescript http://localhost:5080/openapi/v1.json -o src/api/schema.d.ts`)
 * і для цього потрібен ЗАПУЩЕНИЙ бекенд, а він потребує SQL Server.
 * До першого запуску API типів немає — а без модуля не збирається `client.ts`
 * (Q-017). Файл перезаписується генератором і правити його руками не треба.
 */
export type paths = Record<string, never>;
export type components = Record<string, never>;
export type operations = Record<string, never>;
