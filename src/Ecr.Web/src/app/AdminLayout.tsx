import type { JSX } from 'react';
import { Outlet } from 'react-router-dom';

/**
 * Проміжний layout-маршрут секції `/admin/*` (`PR nav-arch #2`).
 *
 * ⚠ Мінімум, явно названий директивою («мінімум `/admin/*`»): усі
 * адміністративні сторінки (шаблони, довідники, методології, безпека,
 * періоди, джерела, мапінг, задачі, знімки, аудит, рядки інтерфейсу,
 * стан здоров'я) монтуються дітьми ЦЬОГО маршруту в `router.tsx`, а не
 * прямими дітьми `AppLayout`. Сам layout — лише `<Outlet />`: рольові
 * гарди (`PR #4`) і заголовок секції/breadcrumbs (`PR #3`) — природне
 * продовження ЦІЄЇ точки дерева, не цієї картки.
 *
 * ⛔ Свідомо БЕЗ візуального контенту (заголовка секції, під-навігації)
 * попри те, що директива згадує «кожен рівень додає свій контекст». Секція
 * `/admin/*` об'єднує 17 РІЗНОРІДНИХ сторінок (шаблони, безпека, задачі…)
 * без спільної під-навігації чи вкладок між ними — єдиний спільний
 * контекст, який тут можна додати чесно, це заголовок «Адміністрування», а
 * він одразу дублював би `PageHeader` кожної дочірньої сторінки, доки немає
 * даних для breadcrumbs (`PR #3`), що відрізняють один рівень від іншого.
 * Картка обмежена вкладеністю маршрутів і `ScrollRestoration` (боундарі
 * PR), не дизайном секцій.
 */
export function AdminLayout(): JSX.Element {
  return <Outlet />;
}
