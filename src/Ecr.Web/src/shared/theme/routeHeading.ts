/**
 * Клас заголовка, на який фокус переносить САМ ЗАСТОСУНОК.
 *
 * ⛔ Константа, а не рядок на місці виклику, і це не педантизм: клас живе в
 * `motion.css`, а вживається у трьох різних місцях (`PageHeader`,
 * `NotFoundPage`, `AccessDeniedPage`). Розсипаний літерал розходиться тихо —
 * перейменували в CSS, у двох файлах із трьох лишилося старе, і кільце в них
 * знову стало гучним, нічого не зламавши.
 *
 * ⚠ Що саме він означає: фокус сюди переніс КОД (на монтуванні маршруту), а не
 * користувач. Не ставити його на цілі, куди користувач приходить свідомо — як
 * `<main>` за посиланням «Пропустити навігацію»: там потрібне звичайне помітне
 * кільце. Розбір — у коментарі над правилом у `motion.css`.
 */
export const RouteHeadingClass = 'ecr-route-heading';
