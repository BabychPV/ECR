import { createTheme, type MantineColorsTuple } from '@mantine/core';

/**
 * Основний колір — кортеж макета `--brand-50…900`
 * (`docs/design/hybrid/index.html:9`), крок `UI-01` директиви №15.
 *
 * ⛔ **Це ЄДИНЕ місце, де живе фірмовий відтінок.** Кольори замовника — факт,
 * якого ми не знаємо (`Q15-01b` у `CUSTOMER-QUESTIONS.md` без відповіді), тож
 * тут стоїть нейтральний за походженням індиго з макета, а `brand[6]`
 * (`#3b3fa8`) свідомо взято ≈ попередньому `#383ca8`: якщо замовник назве свій
 * колір, заміна коштує рівно десяти рядків нижче. Усе інше — `defaultGradient`,
 * кільце фокуса (`motion.css`), `--ecr-accent*` і `--ecr-focus`
 * (`cssVariables.ts`) — виводиться з цього кортежу, а не дублює його.
 *
 * ⚠ Значення перевірені ОБЧИСЛЕННЯМ, не на око; виміряно на нових поверхнях
 * (`surfaces` нижче):
 *   `brand[6]` на білому            8.53  (кільце фокуса, світла тема; треба ≥3)
 *   `brand[6]` на `--ecr-ground`    7.96  (те саме на тлі сторінки;    треба ≥3)
 *   `brand[4]` на `#242424`         4.48  (кільце фокуса, темна тема;  треба ≥3)
 *   `brand[4]` на темному `ground`  5.31  (те саме на тлі сторінки;    треба ≥3)
 *   білий текст на `brand[6]`       8.53  (заливка кнопки, світла;     треба ≥4.5)
 *   білий текст на `brand[5]`       5.81  (заливка кнопки, темна;      треба ≥4.5)
 * Пороги стережуть `__tests__/contrast.test.ts` (перші й останні два рядки) і
 * `__tests__/cssVariables.test.ts` (пари з поверхнями).
 */
export const brand: MantineColorsTuple = [
  '#f4f4fc',
  '#e7e8f8',
  '#cfd1f1',
  '#a9ace3',
  '#7f82d6',
  '#5558c8',
  '#3b3fa8',
  '#2f3290',
  '#24276f',
  '#1a1c50',
];

/**
 * `brand` як ТЕКСТ, межа й посилання в темній темі — ОДНЕ значення.
 *
 * ✎ 2026-09-22, рішення людини «на розсуд команди» (`docs/tz/10-decisions.md`).
 * Mantine у темній схемі брав текст `light`/`subtle` з `brand[0]` (`#f4f4fc`),
 * `outline` — з `brand[1]` (`#e7e8f8`): майже білий, фірмового відтінку немає.
 *
 * ⚠ Значення — той самий відтінок і насиченість, що `brand[6]` (HSL 238°, 48 %),
 * освітлений до L = 78 %: найменший крок, що дає ≥ 4.5 на КОЖНІЙ темній
 * поверхні (`surfaces.dark.*`) і на композитах `light`/hover поверх
 * `ground`/`surface`/`sunken`/`raised`. Виміряно: `ground` 8.67, `surface`
 * 8.01, `sunken` 8.36, `raised` 7.08, `accentSoft` 6.34; найгірше — текст на
 * `light-hover` поверх `raised`, 4.72 (L = 73 %, `#999cdb`, падає: 4.40 на
 * `light-hover` поверх `surface`).
 * З брендбуком замінюється цей рядок; `contrast.test.ts` перевірить новий.
 */
export const brandTextOnDark = '#acaee2';

/**
 * Статусні кольори — `error` і `warning` (`W4.2`).
 *
 * ⛔ Це не перефарбовування. `primaryShade: { light: 6, dark: 5 }` (нижче)
 * діє на КОЖЕН колір теми, не лише на `brand` — підтверджено читанням
 * `getPrimaryShade` у `@mantine/core`: варіанти `filled`/`light`/`outline`
 * будь-якого кольору беруть відтінок за ЦИМ індексом. Стандартна Mantine-
 * шкала `red`/`orange` підібрана під typical `primaryShade.dark` (8), не
 * під 5, тому голе `color="red"` у темній темі досі рендерилось як білий
 * текст на світлому червоному — контраст ~2.2–2.8:1, глибоко нижче AA. Цей
 * дефект був у застосунку ДО цієї зміни, просто жоден тест його не бачив:
 * `contrast.test.ts` перевіряв лише `cellState` і кільце фокуса.
 *
 * ⚠ Значення підібрані обчисленням (не на око), за тим самим методом, що
 * `cellState`: перевірені пари — `filled` (біла мітка на заливці, обидві
 * схеми), `light` (текст на 10%/15%-заливці, обидві схеми) і `outline`
 * (текст/межа на тлі сторінки, обидві схеми). Розрахунок — не в репозиторії
 * (одноразовий скрипт), результат — у `contrast.test.ts`, який і є гейтом.
 */
export const statusError: MantineColorsTuple = [
  '#fff5f5',
  '#ffe3e3',
  '#ffc9c9',
  '#ffa8a8',
  '#ff8787',
  '#c92a2a',
  '#c92a2a',
  '#a61e1e',
  '#a61e1e',
  '#a61e1e',
];

export const statusWarning: MantineColorsTuple = [
  '#fff4e6',
  '#ffe8cc',
  '#ffd8a8',
  '#ffc078',
  '#ffa94d',
  '#a83c0b',
  '#a83c0b',
  '#8a3008',
  '#8a3008',
  '#8a3008',
];

/**
 * Статусний колір — `success` (`Q-262`, злиття `T7-02`+`T7-03`).
 *
 * ⛔ Той самий дефект, що й `error`/`warning` вище, знайдений живим виміром:
 * `<Badge color="green">` (дефолтний `filled`) давав білий текст на заливці з
 * контрастом **2.36:1** (світла тема) і **2.01:1** (темна) — обидва глибоко
 * нижче AA. `<Alert color="green" variant="light">` у світлій темі давав
 * **2.36:1** для тексту на власному 10%-тлі. Причина та сама: стандартна
 * Mantine-шкала `green` підібрана під типовий `primaryShade` (8), не під `6`/
 * `5`, які задає `primaryShade: { light: 6, dark: 5 }` цієї теми.
 *
 * ⚠ Метод виведення — ІДЕНТИЧНИЙ `statusWarning`/`statusError` вище, не
 * винайдений заново: індекси 0–4 лишені дефолтними (не задіяні жодним
 * поточним використанням), індекси 5 і 6 — той самий підібраний перебором
 * колір (один для обох, бо обидва проходять той самий поріг AA.text=4.5),
 * індекси 7–9 — темніший варіант для узгодженості зі шкалою. Перевірені пари
 * — ті самі три, що й для `error`/`warning`: `filled` (біла мітка на заливці,
 * обидві схеми), `light` (текст на 10%/15%-заливці, обидві схеми) і `outline`
 * (текст/межа на тлі сторінки, обидві схеми). Виміряні числа — у
 * `contrast.test.ts`.
 */
export const statusSuccess: MantineColorsTuple = [
  '#ebfbee',
  '#d3f9d8',
  '#b2f2bb',
  '#8ce99a',
  '#69db7c',
  '#1a7431',
  '#1a7431',
  '#124f21',
  '#124f21',
  '#124f21',
];

/**
 * Стани комірки: колір і ФОРМА (`ФВ-14.18`, `D-128`, `D-144`).
 *
 * ⛔ Колір тут — **другий** носій, не перший, і це не риторика, а виміряний
 * факт. Директива №04 §6 порахувала первісну палітру, і виявилося:
 * `calculated` і `rounded` різнилися на ΔE00 = 24.1 для нормального зору і на
 * **5.9** для дейтеранопії. Тобто для кожного шістнадцятого чоловіка два
 * стани, що обидва означають «число змінила система», були майже однакові.
 *
 * ⚠ `ПК1-03` довів більше: **п'яти станів у принципі не розділити самим
 * кольором** для дихромата — перебір усього куба дає стелю близько 14.7.
 * Тому гейт стоїть на `shape`, а не на кольорі.
 *
 * ⛔ Значення нижче — НЕ підібрані на око. Кожне пройшло перебір із трьома
 * обмеженнями (контраст ≥ 3:1 проти власної заливки і проти фону, семантика
 * відтінку, ΔE00 ≥ 20 у нормальному зорі) і виміряне в трьох симуляціях
 * дихромазії. Виміряні числа — у `cellState.test.ts`, вони ж гейти.
 *
 * Що змінилося за `D-144` і чому:
 *
 * | Стан | Було | Стало | Причина |
 * |---|---|---|---|
 * | `calculated` світла | `#1a5aa8` | `#0a4a98` | пара з `rounded` давала ΔE00 18.2 < 20 |
 * | `rounded` світла | `#6b3fb0` | `#8844ff` | дейтеранопія: 1.3 → 11.7 (`ПК1-02`) |
 * | `orphaned` світла | `#b3170c` | `#7a0f08` | пара з `dirty` при дейтеранопії: 2.7 → 11.7 |
 * | `dirty` темна | `#f0b429` | `#ffd24d` | підняти пару з `orphaned` |
 * | `readOnly` темна | `#9e9e9e` | `#9a9a9a` | розвести з `calculated` |
 * | `calculated` темна | `#6faae0` | `#9fd0ff` | пара з `rounded`: 2.0 → 13.7 |
 * | `orphaned` темна | `#f07369` | `#e85c48` | те саме |
 * | `rounded` темна | `#b48ae8` | `#a855f7` | те саме |
 *
 * ⚠ Заливки лишаються блідими навмисно (`ПК1-03`): таблиця з насичених
 * комірок непридатна для восьмигодинної роботи. Розрізнення несе форма.
 */
export const cellState = {
  /** Незбережена правка. */
  dirty: {
    light: { bg: '#fff8e1', line: '#8a6100' },
    dark: { bg: '#3a3320', line: '#ffd24d' },
    marker: 'corner',
    shape: 'solid-bar-corner',
  },

  /** Лише читання за описом шаблону або правами. */
  readOnly: {
    light: { bg: '#f5f5f5', line: '#616161' },
    dark: { bg: '#2a2a2a', line: '#9a9a9a' },
    marker: 'hatch',
    shape: 'hatch',
  },

  /** Рахує система: значення зміниться при наступному перерахунку. */
  calculated: {
    light: { bg: '#eef6ff', line: '#0a4a98' },
    dark: { bg: '#1e2a38', line: '#9fd0ff' },
    marker: 'formula',
    shape: 'dashed-bar-formula',
  },

  /** Рядок утратив посилання на реєстр (`ФВ-8.13`). */
  orphaned: {
    light: { bg: '#fdecea', line: '#7a0f08' },
    dark: { bg: '#3a2422', line: '#e85c48' },
    marker: 'warning',
    shape: 'double-bar-warning',
  },

  /** Значення округлене при вставці (`D-116`). */
  rounded: {
    light: { bg: '#f3e8fd', line: '#8844ff' },
    dark: { bg: '#2e2438', line: '#a855f7' },
    marker: 'dot',
    shape: 'dotted-bar-dot',
  },
} as const;

/**
 * Поверхні й текст макета (`UI-01`, директива №15 §1, рядки `--ground…--hatch`).
 *
 * ⛔ Чому окремий об'єкт, а не набір полів у `createTheme`: Mantine не має
 * поняття «чотири рівні поверхні». `--mantine-color-body` — один, і він
 * дорівнює `ground`; решта (`surface` — панель, `sunken` — заглиблення,
 * `raised` — те, що спливає) існує лише як `--ecr-*`, які видає
 * `cssVariables.ts`. Значення лежать ТУТ, а не в `tokens.css`, бо інакше
 * з'явилося б друге джерело правди, як уже сталося з `cell-states.css`
 * (`ФВ-14.11`).
 *
 * ⚠ Два значення НЕ взяті з макета дослівно, і обидва — з тієї самої причини:
 * макетне не проходить поріг (директива §1 задає правило прямо — «правий той,
 * що проходить `contrast.test.ts`»).
 *
 * 1. `light.faint`: макет дає `#8b91a5`, і воно провалює навіть м'який поріг
 *    3:1 на тлі сторінки — 2.93 на `ground`, 2.75 на `sunken`, 2.75 на
 *    `select`, 2.58 на `accentSoft` (проходить лише на чистому білому — 3.14).
 *    Тут `#7d8496`: 3.49 / 3.28 / 3.27 / 3.08 відповідно. Темне `faint`
 *    (`#727a92`) взято з макета без змін — воно проходить 3:1 на всіх темних
 *    поверхнях (мінімум 3.14 на `accentSoft`).
 * 2. `borderStrong` — межа поля вводу, тобто межа КЕРУВАННЯ, а не роздільник
 *    (`--mantine-color-default-border` бере вивід саме звідси, не з `border`).
 *    ✎ 2026-09-19: макетні `#b9becd`/`#4a516b` замінено. Попередня редакція
 *    цього абзацу називала дефект прямо й лишала його відкритим — «обидва
 *    нижчі за 3:1, закриття вимагає кольору, якого в макеті немає». Рішення
 *    людини: зробити темнішою, доки не пройде 3:1 в ОБОХ темах, порога не
 *    чіпати. Зміряно на всіх восьми поверхнях (`Backdrops` у
 *    `cssVariables.test.ts`):
 *
 *      макетне світле `#b9becd` — 1.53…1.86 (гірше за все на `accentSoft`);
 *      макетне темне  `#4a516b` — 1.71…2.35 (там само);
 *      тут світле `#7b8294` — 3.17…3.84;
 *      тут темне  `#767e97` — 3.33…4.55.
 *
 *    ⚠ Чесно про вибір: «трохи темніше» не існує. Поріг 3:1 на найтіснішій
 *    поверхні (`accentSoft`) виводить межу рівно туди, де вже стоїть `faint`
 *    (контраст між ними — 1.03 світла / 1.06 темна, тобто оком не відрізнити).
 *    Макетна межа була вдвічі світліша за потрібне, а не на крок.
 *    ⛔ Токени НЕ зведені в один попри збіг значень: `faint` — роздільники й
 *    плейсхолдер, `borderStrong` — керування. Спільне значення сьогодні не
 *    означає спільної долі завтра, а зведення зробило б будь-яку зміну одного
 *    тихою зміною другого.
 */
export const surfaces = {
  light: {
    ground: '#f6f7fa',
    surface: '#ffffff',
    sunken: '#eef0f5',
    raised: '#ffffff',
    border: '#dadde6',
    borderStrong: '#7b8294',
    gridLine: '#e3e5ec',
    text: '#1b1e2b',
    muted: '#5b6175',
    faint: '#7d8496',
    accentSoft: '#e7e8f8',
    select: '#eeeffb',
    hover: '#f1f2f7',
    calcBg: '#f3f4f8',
    hatch: '#e6e8ef',
  },
  dark: {
    ground: '#12141c',
    surface: '#191c27',
    sunken: '#151822',
    raised: '#222636',
    border: '#2e3346',
    borderStrong: '#767e97',
    gridLine: '#272b3b',
    text: '#e6e8f0',
    muted: '#9aa1b5',
    faint: '#727a92',
    accentSoft: '#272b55',
    select: '#22264a',
    hover: '#202432',
    calcBg: '#1e2230',
    hatch: '#262a3a',
  },
} as const;

/** Назва поверхні макета. */
export type SurfaceName = keyof typeof surfaces.light;

/**
 * Поверхня й текст **сітки документа** — те, проти чого рахується контраст
 * комірки (`ФВ-14.17`).
 *
 * ⛔ Це НЕ тло сторінки. З `UI-01` тло сторінки задає `surfaces.*.ground`
 * (`--mantine-color-body`), а ці два значення лишилися незмінними навмисно:
 * вони продубльовані в `cell-states.css` (`--ecr-grid-surface`,
 * `--ecr-grid-text`), і саме цю пару читають `contrast.test.ts`,
 * `cellStateMeasured.test.ts` і `gridCellContrast.test.ts`. Зрушити їх тут, не
 * зрушивши `cell-states.css`, означало б, що гейт міряє колір, якого на екрані
 * немає, — рівно той дефект, який ці гейти й ловлять.
 *
 * ⚠ Розбіжність названа, а не замовчана: у темній темі сітка лишається на
 * `#242424`, а сторінка стає `#12141c`. Напрям розбіжності безпечний — реальний
 * контраст ВИЩИЙ за виміряний (`#c9c9c9` на `#12141c` — 9.9 проти 7.2), тож
 * хибнозеленого не буде. Зведення обох джерел — окремий PR, який володіє
 * `cell-states.css` (кроки `UI-03`/`UI-08`).
 */
export const themeSurface = {
  light: { body: '#ffffff', text: '#000000' },
  dark: { body: '#242424', text: '#c9c9c9' },
} as const;

/** Назва стану комірки. */
export type CellStateName = keyof typeof cellState;

/**
 * Тема — **єдине джерело** всіх візуальних значень (`ФВ-14.11`, `D-126`).
 *
 * ⛔ У компонентах не буває жодного літерала: ні `#1a73e8`, ні `padding: 12px`,
 * ні `font-size: 13px`. Причина не в чистоті: п'ятнадцять областей писалися в
 * різний час, і без єдиного джерела вони розійдуться на п'ять відтінків сірого,
 * а привести їх назад буде дорожче, ніж написати заново.
 */
export const theme = createTheme({
  colors: { brand, statusError, statusWarning, statusSuccess },
  primaryColor: 'brand',
  primaryShade: { light: 6, dark: 5 },

  /**
   * Сам «перелив» — від глибокого синього до бузкового.
   *
   * ⚠ Оголошений ТУТ, а не в компонентах: `variant="gradient"` у Mantine без
   * `defaultGradient` змушує кожне місце писати свою пару кольорів, і за
   * півроку їх стає п'ять різних. Те саме правило, що й для решти теми
   * (`ФВ-14.11`).
   *
   * ⛔ Градієнт — оздоба, а не носій змісту: він доречний на шапці чи
   * порожньому стані, але НЕ на тому, що треба прочитати. Текст поверх
   * градієнта не має гарантованого контрасту — його крайні точки
   * (`brand[8]`/`brand[5]`) дають білому 13.17 і 5.81, проте будь-яка
   * проміжна точка не виміряна, а стани комірок сітки мають власні,
   * виміряні за ΔE00 токени (`cellState`) і градієнтом не фарбуються.
   */
  defaultGradient: { from: 'brand.8', to: 'brand.5', deg: 135 },

  /**
   * ✎ 2026-09-19, `Q15-01a` ЗАКРИТО. Попередня редакція цього абзацу стояла
   * на системному стеку **без** `IBM Plex` з прямою причиною: «його заведення
   * це `Q15-01a`, на яке замовник не відповів, а шрифт замовника вигадувати не
   * можна». Відповідь надійшла: IBM Plex дозволено, **лише самохостингом**.
   *
   * ⛔ Самохостинг — не деталь реалізації, а сама умова. Пакети `@fontsource/*`
   * кладуть `.woff2` у власну збірку; жодного запиту на `fonts.googleapis.com`
   * чи інший зовнішній вузол немає. Інакше кожен вхід оператора повідомляв би
   * третій стороні, коли й звідки він увійшов, а в мережі без зовнішнього
   * доступу застосунок малювався б системним шрифтом — тобто інакше, ніж на
   * скріншотах приймання.
   *
   * ⚠ Системний стек лишається ПОЗАДУ, а не зникає, і `system-ui` у ньому —
   * теж умова відповіді. Це не формальність: `font-display: swap` означає, що
   * перший кадр малюється саме цим стеком, доки `.woff2` іде мережею.
   *
   * ⚠ Що змінюється видимо: **усе**. Гарнітура інтерфейсу інша на кожному
   * екрані, і знімки e2e зсунуться — це очікуване, а не регрес.
   *
   * ⚠ `fontFamilyMonospace`: `IBM Plex Mono` попереду тих самих системних
   * моноширинних, що стояли тут раніше (`Cascadia Mono`, `Consolas`,
   * `SFMono-Regular`, `Menlo`), — вони лишаються запобіжником на час
   * завантаження й на випадок, коли `.woff2` не доїхав.
   */
  fontFamily:
    '"IBM Plex Sans", "Segoe UI", system-ui, -apple-system, "Helvetica Neue", Arial, sans-serif',
  fontFamilyMonospace:
    '"IBM Plex Mono", "Cascadia Mono", Consolas, "SFMono-Regular", Menlo, "Courier New", monospace',

  /** Три розміри тексту; основний — `sm` (`ФВ-14.13`). */
  fontSizes: { xs: '11px', sm: '13px', md: '15px', lg: '17px', xl: '20px' },
  lineHeights: { xs: '1.3', sm: '1.45', md: '1.5', lg: '1.5', xl: '1.5' },

  /**
   * Шкала кратна 4 (`ФВ-14.12`). Проміжних значень немає навмисно: око не
   * бачить різниці між 13 і 14 пікселями, але бачить неузгодженість двох
   * сусідніх панелей.
   */
  spacing: { xs: '4px', sm: '8px', md: '12px', lg: '16px', xl: '24px' },

  /**
   * Радіуси макета (`--r1: 4px`, `--r2: 6px`; директива №15 §1).
   *
   * ⚠ `sm` було 3 px — макет не має такого значення взагалі, і `defaultRadius:
   * 'sm'` роздавав його КОЖНОМУ компоненту. Шкала зсунута цілком, а не лише в
   * одній позиції: інакше `sm` (4) збігся б із чинним `md` (4), і вибір між
   * двома сходинками перестав би щось означати — та сама вада, яку
   * `tokens.test.ts` уже гейтить для `spacing`.
   */
  radius: { xs: '2px', sm: '4px', md: '6px', lg: '8px', xl: '12px' },
  defaultRadius: 'sm',

  /**
   * Заголовки (`--fs-h3: 18px`, `--fs-h2: 22px`; директива №15 §1).
   *
   * ⚠ Лише h2 і h3: інших розмірів макет не задає, а вигадувати h1/h4/h5/h6
   * «щоб шкала була повна» означало б поставити в тему числа, яких ніхто не
   * бачив. Mantine лишає для них власні дефолти.
   */
  headings: {
    sizes: {
      h2: { fontSize: '22px' },
      h3: { fontSize: '18px' },
    },
  },

  other: {
    /**
     * Рух ≤ 150 мс (`ФВ-14.27`, `D-129`). Довших значень у темі НЕМАЄ, щоб їх
     * не було й у компонентах: оператор змінює маршрут двісті разів на день, і
     * 300 мс анімації — це п'ять хвилин очікування за тиждень.
     */
    motionFast: '80ms',
    motionBase: '150ms',

    /**
     * Крива руху (`PR nav-arch #7`, директива B6: «тривалості ТА easing —
     * теж токени... а не випадкові числа на кожен компонент»). До цієї
     * картки тривалість (вище) уже була токеном, а крива — ні: усі наявні
     * місця (`Modal`/`Drawer`/`Tooltip` нижче) просто не передавали власної
     * (Mantine бере свою за замовчуванням), тож розходження не було ЩЕ, але
     * перший же новий перехід (маршрутний фейд цієї картки, `routeTransition
     * .css`) поставив би власне число, якби йому не було звідки взяти
     * назване. `ease-out` — свідомо НЕ кастомний cubic-bezier: рух і так
     * ≤150 мс (`ФВ-14.27`), і винаходити криву для проміжку, який людина
     * ледь встигає побачити, — вигадана точність без вигаданої користі.
     */
    motionEasing: 'ease-out',

    /**
     * Дві щільності (`ФВ-14.14`, `D-131`). `compact` за замовчуванням — не
     * економія, а відповідь на «скільки рядків я бачу без прокручування».
     */
    rowHeightCompact: 28,
    rowHeightComfortable: 36,
  },

  components: {
    Button: { defaultProps: { size: 'sm' } },
    TextInput: { defaultProps: { size: 'sm' } },
    Select: { defaultProps: { size: 'sm' } },
    NumberInput: { defaultProps: { size: 'sm' } },
    Table: { defaultProps: { verticalSpacing: 'xs', horizontalSpacing: 'sm' } },

    // ⚠ Тривалість переходу задана ТУТ, а не в кожному діалозі: інакше перший
    // же новий екран поставить свою.
    // ⛔ Аудит-пас 5: кнопка закриття модалки не мала жодного `aria-label`
    // (Mantine не задає дефолтний) — скрінрідер оголошував лише «кнопка»,
    // без жодного натяку на дію. Рядок — статичний, а не через `t()`:
    // `theme` створюється РІВНО ОДИН РАЗ при завантаженні модуля (ФВ-14.11,
    // `App.tsx`), задовго до того, як каталог рядків довантажиться з
    // сервера, — підстановка тут застигла б назавжди як `⟦common.close⟧`.
    // Той самий компроміс, що вже прийнятий для `Breadcrumbs.tsx`
    // (`aria-label="Show all breadcrumbs"`) — текст лише для скрінрідера,
    // не видимий UI-напис.
    Modal: {
      defaultProps: { transitionProps: { duration: 150 }, closeButtonProps: { 'aria-label': 'Close' } },
    },
    // ⛔ Те саме, що в `Modal` вище, і з тієї ж причини — але для сповіщень,
    // і знайдено це вже проходом інтерфейсу як користувач
    // (`docs/build/UI-WALKTHROUGH.md`, F8). Mantine малює хрестик тоста через
    // `<CloseButton>` без тексту й без `aria-label`, тож читалка оголошує
    // «кнопка» — а це ЄДИНА дія, якою сповіщення прибирається з екрана.
    //
    // ⚠ Тут, а не лише в `shared/ui/notify.ts`: одинадцять місць кличуть
    // `notifications.show(...)` повз ту обгортку (`SheetActions.tsx`,
    // `DocumentPage.tsx`, `ExportButton.tsx`, `UserAccessEditor.tsx`,
    // `CreateMappingModal.tsx`, `SourcesPage.tsx`, `PeriodsPage.tsx`,
    // `SnapshotsPage.tsx`, `GrantsPanel.tsx`). Виправлення лише в обгортці
    // закрило б два виклики з тринадцяти і читалося б як «F8 закрито».
    Notification: { defaultProps: { closeButtonProps: { 'aria-label': 'Close notification' } } },
    Drawer: { defaultProps: { transitionProps: { duration: 150 } } },
    Tooltip: { defaultProps: { transitionProps: { duration: 80 } } },
  },
});
