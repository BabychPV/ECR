/**
 * Ключ кешу переліку з'єднань — один на перелік і на всі його правки.
 *
 * ⚠ Окремий модуль, а не експорт `DataSourcesTable`: форма й шухляда, які
 * інвалідизують перелік, самі імпортуються таблицею, і ключ у ній замкнув би
 * коло модулів.
 */
export const DataSourcesQueryKey = ['data-sources'] as const;
