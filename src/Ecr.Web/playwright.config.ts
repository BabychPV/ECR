import { defineConfig, devices } from '@playwright/test';

/**
 * Прогони в справжньому браузері (`D-140`, `D-141`, `D-142`).
 *
 * ⛔ Три речі з ЕТАПУ 7.5 неможливі в jsdom, бо в ньому немає розкладки і
 * немає пікселів:
 *   — структурна різниця станів комірки після знеколірення (`D-140`);
 *   — прохід оператора без миші з перевіркою, що кільце фокуса ВИДНО (`D-141`);
 *   — знімки маршрутів під двома ролями (`D-142`).
 *
 * ⚠ Директива №04 §7 виходила з того, що Playwright уже в наборі. Його не
 * було: на ЕТАПІ 7 доступність зроблена через `axe-core` у jsdom, і це
 * записано в чекпойнті. Тут він з'являється — інакше три пункти звірки
 * лишилися б деклараціями.
 *
 * ⚠ `deviceScaleFactor: 2` обов'язковий для вимірювання: кутовий маркер
 * 6×6 пікселів на одинарному масштабі втрачає половину площі на згладжуванні,
 * і виміряна частка різних пікселів залежала б від антиаліасингу, а не від
 * носія.
 */
export default defineConfig({
  testDir: './e2e',
  fullyParallel: false,

  /*
   * ⛔ Набір без стенда ПАДАЄ, а не виходить нулем. Гейти жили в трьох
   * `describe` як `test.skip`, і `npm run test:e2e` рапортував успіх,
   * виконавши 3 прогони з 20; наслідок описаний у `e2e/globalSetup.ts` —
   * прогони в браузері не виконувалися жодного разу протягом дня, а прилад
   * був зелений.
   *
   * ⚠ `globalSetup` спрацьовує один раз на весь набір, тому новий файл
   * прогонів отримує сторожа задарма — на відміну від гейта в `describe`,
   * який треба не забути скопіювати.
   */
  globalSetup: './e2e/globalSetup.ts',

  // ⛔ Один робітник: сценарій оператора змінює дані в спільній базі, і два
  // паралельні прогони заважали б один одному — падіння виглядало б як
  // випадкове, а такі вимикають.
  workers: 1,
  forbidOnly: true,
  retries: 0,
  reporter: [['list']],

  use: {
    baseURL: 'http://localhost:4173',
    trace: 'retain-on-failure',
    screenshot: 'off',

    // ⛔ Дозвіл на буфер обміну потрібен саме проходу без миші: вставка з
    // Excel — головний шлях введення даних (`ФВ-4.1`), і перевіряти його
    // «майже» немає сенсу. Браузер відмовляє в `writeText` без цього
    // дозволу, і прогін падав би не на своїй причині.
    //
    // ⚠ Дозвіл діє лише в контексті прогону і жодного стосунку до
    // застосунку не має: у браузері користувача вставку ініціює сам
    // користувач, і дозволу для цього не потрібно.
    permissions: ['clipboard-read', 'clipboard-write'],

    /*
     * ⛔ Без цього рядка дія успадковує БЮДЖЕТ ПРОГОНУ. Заміряно на живому
     * стенді: `validate.click()` (`zz-walkthrough.spec.ts`) чекав **579.8 с**
     * із 600 с бюджету WALK 2, бо оверлей модалки перехоплював вказівник
     * (`intercepts pointer events`). Відмова прийшла на десять хвилин пізніше
     * причини і в іншому місці — найдорожчий різновид: читається як зависання
     * продукту, а не як заблокований клік.
     *
     * ⚠ Число взято за НАЙПОВІЛЬНІШОЮ ЗАКОННОЮ дією набору, а не зі стелі.
     * Нею є `page.screenshot({ fullPage: true })` у `screenshots.spec.ts`:
     * `/admin/ui-strings` рендерить 1314 рядків однією таблицею (1280×49 641
     * CSS-пікселів), тож при `deviceScaleFactor: 2` це 254 млн пікселів і —
     * заміряно, не оцінено (шапка `screenshots.spec.ts`) — **19.4 с** на один
     * знімок. Решта дій на порядок швидші: явні `timeout` у розвідувальних
     * викликах — 3–10 с (`isEnabled`, `innerText`, `grid.focus`), а кліки
     * йдуть після `toBeVisible`, тобто по вже видимому елементу.
     *
     * ⚠ 45 с — це 2.3× від 19.4 с. Тісніше (30 с) не лишає запасу на
     * повільнішу машину й завалило б знімок, який нічим не завинив; ширше
     * (60 с) — це вже десять хвилин на шість заблокованих кроків, тобто
     * повернення до того самого «відмова приходить пізно». Заблокований клік
     * тепер падає за 45 с замість 579.8 с — у 13 разів раніше й у своєму
     * кроці.
     *
     * ⛔ На `expect(...).toBeVisible({ timeout: 60_000 })` це НЕ поширюється:
     * `actionTimeout` керує ДІЯМИ, а не `expect`, тож 60-секундні очікування
     * сітки (`lazyTables.spec.ts`, `zz-walkthrough.spec.ts`) лишаються, якими
     * були. `page.goto` теж не зачеплено — у нього власний `navigationTimeout`.
     */
    actionTimeout: 45_000,
  },

  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'], deviceScaleFactor: 2 },
    },
  ],

  /*
   * ⛔ Саме DEV-сервер, а не `preview`. Стенд для вимірювання живе на
   * `/_kitchen-sink`, а цього маршруту у виробничій збірці НЕМАЄ за
   * побудовою (`D7-09`) — і це правильно: сторінка тягне всі спільні
   * компоненти одразу. Піднімати `preview` означало б або відкрити стенд
   * замовникові, або вимірювати те, чого там немає.
   *
   * ⚠ CSS у розробці не мініфікований, але значення ті самі: мініфікація не
   * змінює кольорів і меж, а саме їх ми й міряємо.
   */
  webServer: {
    command: 'npm run dev -- --port 4173 --strictPort',
    url: 'http://localhost:4173',
    reuseExistingServer: true,
    timeout: 120_000,
  },
});
