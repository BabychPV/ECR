import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';
import path from 'node:path';

/**
 * Окремий прогін перевірки доступності (`ФВ-14.16`, `D-127`).
 *
 * ⛔ Окремий НЕ тому, що перевірка необов'язкова — вона блокує CI, — а тому,
 * що вона повільна: `axe` у jsdom обробляє одну сторінку Mantine близько
 * 35 секунд, тобто сім хвилин на дванадцять маршрутів. Повільна перевірка
 * всередині `npm test` робить повільним КОЖЕН прогін під час роботи, а те, що
 * заважає щохвилини, зрештою вимикають.
 *
 * ⚠ Причина повільності — не наші правила: звуження переліку і
 * `resultTypes: ['violations']` не змінили нічого. `axe` викликає
 * `getComputedStyle` на кожному вузлі, а в jsdom це найдорожча операція, яка
 * до того ж перечитує весь набір CSS-змінних теми.
 *
 * ⛔ Конфіг самостійний, а не `mergeConfig(base, …)`: `mergeConfig` ЗЛИВАЄ
 * масиви, тож `exclude` базового конфігу — а він виключає саме `*.a11y.*` —
 * додався б сюди і викинув би єдині файли, заради яких цей конфіг існує.
 * Перевірено: «No test files found».
 *
 * Запуск: `npm run test:a11y`; входить у `tools/verify-all.ps1`.
 */
export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: { '@': path.resolve(__dirname, './src') },
  },
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/test/setup.ts'],
    include: ['src/**/*.a11y.test.{ts,tsx}'],
    exclude: ['**/node_modules/**', '**/dist/**'],

    // ⛔ Межа піднята зі 120 с після заміру 2026-09-07: на цій машині три
    // маршрути падали НЕ на порушеннях, а на таймауті — `/admin/expressions`
    // 158 с наодинці і 230 с у повному прогоні, `/_kitchen-sink` 205 с,
    // `/admin/registries` 189 с. Жоден із них не назвав жодного порушення:
    // `axe` просто не встигав добігти.
    //
    // ⚠ Це найгірший різновид червоного гейта — той, що червоніє не з тієї
    // причини, яку стереже. Він навчає читати «a11y впав» як «машина
    // повільна», і перше справжнє порушення `critical` проїде тим самим
    // рядком. Число має відображати ЗАМІР, а не давню оцінку «~35 с».
    //
    // ⚠ Ціна названа: повний прогін іде ~26 хв. Тому він і винесений з
    // `npm test` в окремий конфіг.
    testTimeout: 400_000,
    hookTimeout: 400_000,

    // ⛔ `Q-270`. Набір розбитий на чотири файли
    // (`src/test/__tests__/accessibility.part{1..4}.a11y.test.tsx`, по шість
    // маршрутів кожен) САМЕ тому, що Vitest розпаралелює ФАЙЛИ, а не
    // `it.each` усередині одного файлу — попередній єдиний файл (24
    // маршрути) не міг отримати жодного виграшу від будь-якого значення
    // `maxWorkers`, бо був один. Тіла тестів і перелік маршрутів не
    // змінені — розбиття суто механічне (див. коментар у кожному
    // `part*.a11y.test.tsx` і спільні приладдя в
    // `src/test/__tests__/a11yFixtures.tsx`).
    //
    // ⚠ `maxWorkers: 2`, а не 3 і не 4 (тут `ubuntu-latest`, 4 vCPU): коментар
    // вище в цьому ж файлі вже документує, що конкуренція за ресурси на
    // САМЕ цьому наборі проявляється РАНІШЕ, ніж на повних чотирьох ядрах —
    // це і є причина, з якої колись підняли `testTimeout`/`hookTimeout` до
    // 400_000 мс (три маршрути падали на таймауті, не на порушеннях,
    // D-127/W4.3). Два воркери — це два процеси `axe`/jsdom одночасно
    // максимум, а не чотири: лишається запас ядер для Node/Vite-оркестрації
    // й системних процесів раннера, а не змагання за всі чотири одразу.
    //
    // ⚠ Три й чотири воркери НЕ перевірялися окремим заміром (Q-270 називає
    // це прямо, а не мовчить): це свідомо консервативний вибір НА ОСНОВІ вже
    // задокументованого в D-127/W4.3 факту (конкуренція видима на цьому наборі
    // за менших ніж повні чотири ядра навантаженнях), а не результат
    // перебору 2/3/4 на цій машині. Якщо наступний замір покаже, що три
    // воркери не відтворюють той самий ефект — це окреме число, зі своїм
    // заміром, а не розширення цього коментаря заднім числом.
    maxWorkers: 2,
  },
});
