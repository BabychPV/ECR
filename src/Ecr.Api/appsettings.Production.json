{
  // Заповнюється на майданчику розгортання, не тут. Порожній об'єкт —
  // свідомий дефолт (docs/build/10-installer.md, п. про AppSettingsProd):
  // інсталятор кладе копію цього файлу в %ProgramData%\ECR\config один раз
  // і більше не перезаписує при оновленнях (NeverOverwrite), а Program.cs
  // читає САМЕ ту копію (AddJsonFile programDataConfigPath) — тож
  // адміністратор редагує ЇЇ, не файл поруч з Ecr.Api.exe (той
  // перезаписується заповнювачем при кожному оновленні MSI).
  //
  // ⛔ СЕКРЕТИ СЮДИ НІКОЛИ (D-11, docs/build/04-environment.md §6) —
  // рядок підключення до SQL Server і подібне заводяться ЛИШЕ через змінні
  // оточення служби (ECR_ConnectionStrings__Ecr у реєстрі
  // HKLM:\SYSTEM\CurrentControlSet\Services\EcrApi\Environment,
  // tools/deploy-ecr.ps1 -ConnectionString). Тут — лише НЕсекретні
  // значення конкретного майданчика (наприклад, чи потрібен
  // OTLP-колектор): факти замовника, яких код наперед не вигадує.
}
