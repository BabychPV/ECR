<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Ecr.TestKit</RootNamespace>
    <IsPackable>false</IsPackable>
    <!-- Q-022: це бібліотека фікстур, а не тестовий проєкт. Без цього рядка
         `dotnet test Ecr.sln` намагається запустити її як тестову збірку,
         не знаходить раннера і завершує ВЕСЬ прогін через Test Run Aborted. -->
    <IsTestProject>false</IsTestProject>
    <!-- XML-doc у тестових хелперах не вимагаємо -->
    <NoWarn>$(NoWarn);CS1591</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Ecr.Domain\Ecr.Domain.csproj" />
    <ProjectReference Include="..\..\src\Ecr.Application\Ecr.Application.csproj" />
    <ProjectReference Include="..\..\src\Ecr.Infrastructure\Ecr.Infrastructure.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="xunit" />
    <PackageReference Include="NSubstitute" />
    <PackageReference Include="Testcontainers.MsSql" />
    <!-- ⛔ `Microsoft.EntityFrameworkCore.Sqlite` прибрано: `UseSqlite` не мав
         у `tests/` жодного місця виклику. Усі тести з базою йдуть через
         `SqlServerFixture` на справжній SQL Server, і посилання лише
         підтримувало думку, що десь є дешевший провайдер «під тести». -->
  </ItemGroup>

  <ItemGroup>
    <None Update="Fixtures\*.json" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
